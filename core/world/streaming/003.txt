reviewed++;if(this.previewCache.size>BOUNDS.totalChunks)this.previewCache.delete(this.previewCache.keys().next().value);return c}
 visibleChunks(camera,w,h){this.clampCamera(camera,w,h);const halfW=w/(2*camera.zoom)+CS,halfH=h/(2*camera.zoom)+CS,a=Math.max(BOUNDS.minCx,AZ.chunkCoord(camera.x-halfW)),b=Math.min(BOUNDS.maxCx,AZ.chunkCoord(camera.x+halfW)),c=Math.max(BOUNDS.minCy,AZ.chunkCoord(camera.y-halfH)),d=Math.min(BOUNDS.maxCy,AZ.chunkCoord(camera.y+halfH)),ccx=AZ.chunkCoord(camera.x),ccy=AZ.chunkCoord(camera.y),out=[];for(let cy=c;cy<=d;cy++)for(let cx=a;cx<=b;cx++){const loaded=this.peekChunk(cx,cy);if(loaded){loaded.lastUsed=performance.now();out.push(loaded);continue}const dist=Math.max(Math.abs(cx-ccx),Math.abs(cy-ccy)),detailRadius=camera.zoom<.28?1:camera.zoom<.48?2:3;if(dist<=detailRadius)this.requestChunk(cx,cy,Math.max(2,5-dist),'camera');const p=this._preview(cx,cy);if(p)out.push(p)}return out}
 nearbySites(x,y,radius=500){const out=[],r2=radius*radius,minX=Math.max(BOUNDS.minCx,AZ.chunkCoord(x-radius)),maxX=Math.min(BOUNDS.maxCx,AZ.chunkCoord(x+radius)),minY=Math.max(BOUNDS.minCy,AZ.chunkCoord(y-radius)),maxY=Math.min(BOUNDS.maxCy,AZ.chunkCoord(y+radius));if(minX>maxX||minY>maxY)return out;for(let cy=minY;cy<=maxY;cy++)for(let cx=minX;cx<=maxX;cx++){const c=this.peekChunk(cx,cy);if(!c){this.requestChunk(cx,cy,3,'sensor-sites');continue}for(const s of c.sites||[]){const px=Math.max(s.x,Math.min(x,s.x+s.w)),py=Math.max(s.y,Math.min(y,s.y+s.h));if((px-x)**2+(py-y