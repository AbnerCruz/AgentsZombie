ame.set(nid,qid);open.push({x:nx,y:ny,g:ng,f:ng+Math.hypot(tc.x-nx,tc.y-ny)})}}if(!came.has(tk))return[];const rev=[];let id=tk;while(id!==sk){const[x,y]=id.split(',').map(Number);rev.push({cx:x,cy:y,x:(x+.5)*CS,y:(y+.5)*CS});id=came.get(id);if(!id)break}return rev.reverse()}
 planRoute(sx,sy,tx,ty,actor=null){const t=this.clampPoint(tx,ty,C*1.5);return super.planRoute(sx,sy,t.x,t.y,actor)}
 entityByWorldId(id){if(!id)return null;let m=String(id).match(/^v:(-?\d+):(-?\d+):/);if(m){if(!this.chunkInBounds(+m[1],+m[2]))return null;const c=this.getChunk(+m[1],+m[2]);return(c.vehicles||[]).find(v=>v.id===id)||null}m=String(id).match(/^(?:ct|op):(-?\d+):(-?\d+):/);if(m){if(!this.chunkInBounds(+m[1],+m[2]))return null;const c=this.getChunk(+m[1],+m[2]);for(const s of c.sites||[])for(const e of [...(s.containers||[]),...(s.doors||[]),...(s.windows||[])])if(e.id===id)return e;return null}m=String(id).match(/^(-?\d+):(-?\d+):/);if(m){if(!this.chunkInBounds(+m[1],+m[2]))return null;const c=this.getChunk(+m[1],+m[2]);return(c.sites||[]).find(s=>s.id===id)||null}return null}
 openingAtCell(q){const c=q.chunk;if(!c||c.preview||c.void)return null;if(!c._openingIndex){const idx=c._openingIndex=new Map();for(const s of c.sites||[])for(const o of [...(s.doors||[]),...(s.windows||[])]){const lx=Math.floor((o.x-c.cx*CS)/C),ly=Math.floor((o.y-c.cy*CS)/C);idx.set(`${lx},${ly}`,o)}}return c._openingIndex.get(`${q.lx},${q.ly}`)||null}
 chunkEdges(cx,cy){if(!this.chunkInBounds(cx,cy))return{N:false,E