maxY-hh;camera.x=minX<=maxX?AZ.clamp(camera.x,minX,maxX):(BOUNDS.minX+BOUNDS.maxX)/2;camera.y=minY<=maxY?AZ.clamp(camera.y,minY,maxY):(BOUNDS.minY+BOUNDS.maxY)/2;return camera}
 peekChunk(cx,cy){if(!this.chunkInBounds(cx,cy))return null;return this.chunks.get(key(cx,cy))||this.touched.get(key(cx,cy))||null}
 requestChunk(cx,cy,priority=1,reason='view'){if(!this.chunkInBounds(cx,cy)){this.generationStats.rejected++;return false}const k=key(cx,cy);if(this.peekChunk(cx,cy))return false;const now=performance.now(),old=this.generationQueue.get(k);if(old){if(priority>old.priority){old.priority=priority;old.reason=reason}old.requestedAt=now;return false}this.generationQueue.set(k,{cx,cy,priority,reason,requestedAt:now});this.generationStats.queued=this.generationQueue.size;return true}
 _recordGeneration(ms,bg){const s=this.generationStats;s.generated++;if(bg)s.background++;else s.synchronous++;s.totalMs+=ms;s.lastMs=ms;s.maxMs=Math.max(s.maxMs,ms);s.queued=this.generationQueue.size}
 getChunk(cx,cy){if(!this.chunkInBounds(cx,cy))return this._voidChunk;const k=key(cx,cy),existing=this.chunks.get(k);if(existing){existing.lastUsed=performance.now();return existing}const touched=this.touched.get(k);if(touched){touched.lastUsed=performance.now();this.chunks.set(k,touched);this.touched.delete(k);this.generationQueue.delete(k);this.previewCache.delete(k);return touched}const t=performance.now(),c=super.getChunk(cx,cy),ms=performance.now()-t;this.generationQueue.delete(k);this.previewCache