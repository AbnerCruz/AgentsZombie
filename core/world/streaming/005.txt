.worldInBounds(x,y,C*.2))return true;return super.isCellBlocked(gx,gy,actor,allowClosedDoor)}
 isSolidAt(x,y,actor=null){if(!this.worldInBounds(x,y,C*.3))return true;return super.isSolidAt(x,y,actor)}
 nearestWalkable(x,y,actor=null,max=10){const p=this.clampPoint(x,y,C*1.5);return super.nearestWalkable(p.x,p.y,actor,max)}
 findLocalPath(sx,sy,tx,ty,actor=null,opts={}){const s=this.clampPoint(sx,sy,C*1.5),t=this.clampPoint(tx,ty,C*1.5);return super.findLocalPath(s.x,s.y,t.x,t.y,actor,opts)}
 coarseRoute(sx,sy,tx,ty){const s=this.clampPoint(sx,sy,C*1.5),t=this.clampPoint(tx,ty,C*1.5),sc={x:AZ.chunkCoord(s.x),y:AZ.chunkCoord(s.y)},tc={x:AZ.chunkCoord(t.x),y:AZ.chunkCoord(t.y)},sk=key(sc.x,sc.y),tk=key(tc.x,tc.y);if(sk===tk)return[];const open=[{x:sc.x,y:sc.y,g:0,f:Math.hypot(tc.x-sc.x,tc.y-sc.y)}],g=new Map([[sk,0]]),came=new Map(),closed=new Set(),dirs=[[1,0,'E','W'],[-1,0,'W','E'],[0,1,'S','N'],[0,-1,'N','S']];let guard=0;while(open.length&&guard++<BOUNDS.totalChunks*8){let bi=0;for(let i=1;i<open.length;i++)if(open[i].f<open[bi].f)bi=i;const q=open.splice(bi,1)[0],qid=key(q.x,q.y);if(closed.has(qid))continue;closed.add(qid);if(qid===tk)break;const e=this.chunkEdges(q.x,q.y);for(const[dx,dy,a,b]of dirs){const nx=q.x+dx,ny=q.y+dy;if(!this.chunkInBounds(nx,ny))continue;const nid=key(nx,ny),ne=this.chunkEdges(nx,ny),road=e[a]&&ne[b],zone=W.zoneAt(this,(nx+.5)*CS,(ny+.5)*CS),step=road?1:zone==='wilderness'?4.8:2.6,ng=q.g+step;if(ng>=(g.get(nid)??Infinity))continue;g.set(nid,ng);c