rimentos graves e comportamento anormal',recognition:knew?'ameaça anormal reconhecida':'não compreendido'});
 const sites=sensedSites(a,range),inside=sensedInterior(a,range),vehicles=sim.world.nearbyVehicles(a.x,a.y,range*1.1).filter(v=>sim.canSee(a,v,range*1.1)).slice(0,6).map(v=>({id:v.id,name:v.name,distance:V.qual.distance(Math.hypot(v.x-a.x,v.y-a.y)),direction:dirName(v.x-a.x,v.y-a.y),condition:v.condition>65?'aparenta funcionar':v.condition>30?'aparenta danificado':'muito danificado'}));if(!passive){for(const s of sim.world.nearbySites(a.x,a.y,range).filter(s=>sim.canSee(a,{x:s.x+s.w/2,y:s.y+s.h/2},range)))V.learnPlace(a,s)}const hearing=hearingFor(a),own=a.vehicleId?'alto':a.action?.tool?.includes('breach')?'muito alto':a.action?.tool==='attack'?'moderado':a.action?'baixo':'quase silencioso';return{vision:{facing:dirName(Math.cos(a.facing||0),Math.sin(a.facing||0)),people:people.slice(0,12),unidentified_figures:figures,sites,vehicles,containers:inside.containers,openings:inside.openings,room:inside.room},hearing,own_noise:own,terrain:sim.world.terrainAt(a.x,a.y),weather:weather.label,light:lighting(a)<.3?'escuro':lighting(a)<.65?'penumbra':'claro'}};
sim.observeSense=p=>sim.senseAgent(p,true);sim.sense=p=>sim.senseAgent(p,false);
function markEvent(p,reason,urgent=false){if(p.type==='agent'&&V.markReplan)V.markReplan(p,reason,urgent)}
function ensureNav(p){return p._nav||(p._nav={goal:null,path:[],coarse:[],index:0,stuck:0,lastX:p.x,lastY:p.y,lastRepath:-999,lastStepNoise:-999})}
function clearNav(p){p._nav=null}
function targetFor(p,a){if(a?.args?.target_id){const t=sim.entityById(a.args.target_id);if(t)return{x:t.x+(t.w||0)/2,y:t.y+(t.h||0)/2,entity:t}}if(a?.args?.vehicle_id){const t=sim.world.vehicleById(a.args.vehicle_id);if(t)return{x:t.x,y:t.y,entity:t}}if(a?.args?.site_id){const t=sim.findSiteById(a.args.site_id);if(t)return{x:t.x+t.w/2,y:t.y+t.h/2,entity:t}}if(a?.tool==='flee'&&a.args?.threat_id){const t=sim.entityById(a.args.threat_id);if(t){const dx=p.x-t.x,dy=p.y-t.y,l=Math.hypot(dx,dy)||1;return{x:p.x+dx/l*520,y:p.y+dy/l*520,entity:null}}}return p.target?{x:p.target.x,y:p.target.y,entity:null}:null}
function ensureRoute(p,goal){const nav=ensureNav(p),changed=!nav.goal||Math.hypot(nav.goal.x-goal.x,nav.goal.y-goal.y)>28||!nav.path?.length;if(changed||sim.minute-nav.lastRepath>12&&nav.stuck>0){const r=sim.world.planRoute(p.x,p.y,goal.x,goal.y,p);nav.goal={x:goal.x,y:goal.y};nav.path=r.path||[];nav.coarse=r.coarse||[];nav.index=0;nav.stuck=0;nav.lastRepath=sim.minute;sim._v9.pathReplans++}return nav}
function maybeDoor(p,wp){const q=sim.world.cellAtWorld(wp.x,wp.y);if(q.code!==O.DOOR_CLOSED)return true;const op=sim.world.openingAtCell(q);if(op&&!op.locked&&(op.barricade||0)<=0){sim.world.setOpening(op,true);sim.emitNoise?.(op.x,op.y,.08,'door',p.id,'porta abrindo');return true}if(op){sim.remember(p,'barrier',`Uma ${op.openingType==='window'?'janela':'porta'} bloqueia o caminho${op.locked?' e parece trancada':''}.`,5,['barrier',op.id]);markEvent(p,'barrier_blocked',false)}return false}
function completeMove(p,a){const args={...(a.args||{})};if(a.tool==='npc_seek'){p.action=null;p.target=null;clearNav(p);sim.npcScavenge?.(p,args.site_id);return}if(args._phase==='approach'){delete args._phase;p.action=null;p.target=null;clearNav(p);sim.executeAgentTool(p,a.tool,args,{model:a.model||'local-intent',forceImmediate:true,localInte