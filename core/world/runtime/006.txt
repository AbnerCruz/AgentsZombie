a.args.container_id);if(co){const chance=clamp(.38+sim.skillLevel(p,'mechanics')*.07+(p.attr?.strength||4)*.025,.42,.95);sim.practice(p,'mechanics',2);if(sim.rng.chance(chance)){co.locked=false;co.dirty=true;sim.remember(p,'scavenge',`Conseguiu abrir ${co.name}.`,4,['scavenge',co.id])}else{co.integrity=Math.max(1,(co.integrity||45)-8);sim.remember(p,'scavenge',`Não conseguiu abrir ${co.name} nesta tentativa.`,3,['scavenge',co.id])}}return}
 if(a.tool==='search_container_v9'){const co=sim.world.containerById(a.args.container_id);if(co)transferFromContainer(p,co,a.args.wanted);return}
 if(a.tool==='breach_v9'){const op=sim.world.openingById(a.args.opening_id);if(op){const w=sim.weaponStats(p),power=16+(p.attr.strength||4)*3+sim.skillLevel(p,'mechanics')*2+(a.args.method==='weapon'?w.damage*.45:0);sim.world.damageOpening(op,power);sim.practice(p,'mechanics',2);sim.emitNoise?.(op.x,op.y,.9,'breach',p.id,'barreira arrombada');if(op.integrity>0)sim.remember(p,'barrier','A barreira cedeu parcialmente, mas ainda está de pé.',4,['barrier',op.id])}return}
 if(a.tool==='barricade_v9'){const op=sim.world.openingById(a.args.opening_id);if(op&&sim.takeItem(p,'wood',2)){op.barricade=clamp((op.barricade||0)+.65,0,1);op.integrity+=35;op.dirty=true;sim.practice(p,'carpentry',8);sim.emitNoise?.(op.x,op.y,.42,'construction',p.id,'marteladas e madeira');sim.remember(p,'build','Barricou uma abertura específica.',6,['build',op.id])}return}
 if(a.tool==='construction_v9'){
  if(sim.consumeNeed(p,a.args.needs)){const q=sim.world.cellAtWorld(p.x+Math.cos(p.facing||0)*22,p.y+Math.sin(p.facing||0)*22);const st={kind:'structure',id:`st${sim.nextId++}`,type:a.args.structure,x:q.x,y:q.y,owner:p.id,integrity:100,created:sim.minute};sim.structures.push(st);if(a.args.structure==='wall')sim.world.setCellWorld(q.x,q.y,O.WALL);if(a.args.structure==='trap')sim.world.setCellWorld(q.x,q.y,O.RUBBLE);sim.practice(p,'carpentry',10);sim.emitNoise?.(q.x,q.y,.5,'construction',p.id,'trabalho de construção')}
  return
 }
 return oldFinish(p,a)
};
sim.npcScavenge=function(p,siteId){const s=sim.findSiteById(siteId),co=pickContainer(s,p.thirst>p.hunger?'water':'food');if(co)return startSearch(p,co,{wanted:p.thirst>p.hunger?'water':'food',reason:'procurar suprimentos'},'npc');return oldNpcScavenge?.(p,siteId)};
function depositScent(p){if(!p.alive)return;const q=sim.world.cellAtWorld(p.x,p.y),k=`${q.gx},${q.gy}`,old=sim.scentMap.get(k);sim.scentMap.set(k,{x:q.x,y:q.y,minute:sim.minute,strength:Math.min(1,(old?.strength||0)*.55+.55)});if(sim.scentMap.size>2400){const cutoff=sim.minute-90;for(const[k,v]of sim.scentMap)if(v.minute<cutoff)sim.scentMap.delete(k)}}
function sensorPulse(p){if(p.type!=='agent'||!p.alive)return;const t=performance.now();if(t-(p._v9SenseAt||0)<480)return;p._v9SenseAt=t;const s=sim.senseAgent(p,false),ids=new Set((s.vision.unidentified_figures||[]).map(x=>x.id)),prev=p._v9Threats||new Set();for(const id of ids)if(!prev.has(id)){const z=sim.entityById(id),urgent=z&&Math.hypot(z.x-p.x,z.y-p.y)<95;markEvent(p,'new_spatial_threat',urgent)}p._v9Threats=ids;const loud=s.hearing.find(h=>h._power>.35);if(loud&&loud.id!==p._v9LastLoud){p._v9LastLoud=loud.id;markEvent(p,'significant_sound',false)}}
sim.updatePerson=function(p,dt){oldUpdatePerson(p,dt);if(!p.alive)return;if(sim.minute-(p._v9ScentAt||-999)>.8){p._v9ScentAt=sim.minute;depositScent(p)}sensorPulse(p)};
function sce