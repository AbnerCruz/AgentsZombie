nt:true});return}p.action=null;p.target=null;clearNav(p)}
function navAdvance(p,a,dt){const goal=targetFor(p,a);if(!goal){p.action=null;clearNav(p);return}const threshold=(a.args?._phase==='approach'?Math.max(24,sim.weaponStats(p).range*.75):18),gd=Math.hypot(goal.x-p.x,goal.y-p.y);if(gd<=threshold){completeMove(p,a);return}const nav=ensureRoute(p,goal);if(nav.index>=nav.path.length){nav.goal=null;ensureRoute(p,goal)}let wp=nav.path[nav.index]||goal;if(!maybeDoor(p,wp)){p.action=null;p.target=null;clearNav(p);return}if(Math.hypot(wp.x-p.x,wp.y-p.y)<10){nav.index++;wp=nav.path[nav.index]||goal}const dx=wp.x-p.x,dy=wp.y-p.y,d=Math.hypot(dx,dy)||1,terrain=sim.world.movementCostAt(p.x,p.y,p),run=/flee|reflex/.test(a.tool)||a.args?.pace==='run',crouch=a.args?.pace==='crouch',carry=inventoryPenalty(p),base=(35+(p.attr?.agility||4)*2.35+sim.skillLevel(p,'sprinting')*1.5)*(run?1.42:crouch?.58:1)*(1-carry*.48)*(p.bodyClock?.mobility||1),speed=Math.max(7,base/Math.max(.58,terrain));let vx=dx/d*speed*dt,vy=dy/d*speed*dt;let ax=0,ay=0;for(const q of sim.peopleGrid.near(p.x,p.y,22)){if(q.id===p.id||!q.alive)continue;const rx=p.x-q.x,ry=p.y-q.y,rd=Math.hypot(rx,ry)||1;if(rd<15){ax+=rx/rd*(15-rd)*.18;ay+=ry/rd*(15-rd)*.18}}vx+=ax;vy+=ay;const m=sim.world.tryMove(p.x,p.y,vx,vy,5,p);if(m.moved){p.x=m.x;p.y=m.y;p.facing=Math.atan2(vy,vx);const moved=Math.hypot(p.x-nav.lastX,p.y-nav.lastY);if(moved>.7){nav.stuck=0;nav.lastX=p.x;nav.lastY=p.y}else nav.stuck+=dt;const noise=(run?.14:crouch?.015:.045)*(1+carry*.8);if(sim.minute-nav.lastStepNoise>.18){nav.lastStepNoise=sim.minute;sim.emitNoise?.(p.x,p.y,noise,'footstep',p.id,run?'passos correndo':'passos')}p.energy=clamp(p.energy-dt*(run?.026:.012),0,100)}else{sim._v9.collisions++;nav.stuck+=dt;if(nav.stuck>1.4){nav.goal=null;nav.path=[];nav.lastRepath=-999}if(nav.stuck>4.5){p.action=null;p.target=null;clearNav(p);markEvent(p,'route_blocked',false)}}}
sim.advanceAction=function(p,dt){const a=p.action;if(a&&(MOVE.has(a.tool)||a.args?._phase==='approach')&&a.tool!=='drive_vehicle')return navAdvance(p,a,dt);return oldAdvance(p,dt)};
sim.advanceDrive=function(p,a,dt){const v=p.vehicleId&&sim.world.vehicleById(p.vehicleId);if(!v||v.occupant!==p.id){p.action=null;p.vehicleId=null;clearNav(p);return}if(v.fuel<=0||v.condition<=3){p.action=null;markEvent(p,'vehicle_disabled');return}const goal=p.target||targetFor(p,a);if(!goal){p.action=null;return}const nav=ensureRoute(p,goal),gd=Math.hypot(goal.x-v.x,goal.y-v.y);if(gd<20){p.action=null;p.target=null;clearNav(p);return}if(nav.index>=nav.path.length){nav.goal=null;ensureRoute(p,goal)}let wp=nav.path[nav.index]||goal;if(Math.hypot(wp.x-v.x,wp.y-v.y)<14){nav.index++;wp=nav.path[nav.index]||goal}const dx=wp.x-v.x,dy=wp.y-v.y,d=Math.hypot(dx,dy)||1,road=sim.world.terrainAt(v.x,v.y)==='road',speed=(road?185:72)*(v.condition/100)*(.76+sim.skillLevel(p,'driving')*.035),step=Math.min(d,speed*dt),m=sim.world.tryMove(v.x,v.y,dx/d*step,dy/d*step,11,p);if(!m.moved){nav.stuck+=dt;if(nav.stuck>1.5){nav.goal=null;nav.path=[]}if(nav.stuck>4){p.action=null;markEvent(p,'vehicle_route_blocked');clearNav(p)}return}nav.stuck=0;if(v.occ){const oq=sim.world.cellByGlobal(v.occ.gx,v.occ.gy);if(oq.code===O.VEHICLE)oq.chunk.occ[oq.ly*W.GRID+oq.lx]=v.underCode??O.ROAD}v.x=m.x;v.y=m.y;const nq=sim.world.cellAtWorld(v.x,v.y);v.underCode=nq.code===O.VEHICLE?O.ROAD:nq.code;nq.chunk.occ[nq.ly*W.GRID+nq