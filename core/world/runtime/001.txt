urn sim.world.rayClear(a.x,a.y,tx,ty,true)};
function noiseBand(v){return v>.85?'muito alto':v>.45?'alto':v>.2?'moderado':'baixo'}
function hearingFor(a){const out=[],weather=sim.world.weatherAt(sim.minute);sim.noiseEvents=sim.noiseEvents.filter(n=>n.expires>sim.minute);for(const n of sim.noiseEvents){if(n.sourceId===a.id)continue;const d=Math.hypot(n.x-a.x,n.y-a.y),trans=sim.world.lineTransmission(a.x,a.y,n.x,n.y),effective=n.loudness*trans*(1-weather.rain*.25),reach=85+effective*(520+(a.attr?.perception||4)*40);if(d>reach||effective<.025)continue;out.push({id:n.id,sound:n.label||n.kind,direction:dirName(n.x-a.x,n.y-a.y),distance:V.qual.distance(d),intensity:noiseBand(effective),_d:d,_power:effective})}return out.sort((x,y)=>y._power-x._power).slice(0,10)}
function sensedSites(a,range){const sites=[];for(const s of sim.world.nearbySites(a.x,a.y,range*1.25)){const cx=s.x+s.w/2,cy=s.y+s.h/2;if(!sim.canSee(a,{x:cx,y:cy},range*1.25)&&sim.currentSite?.(a)?.id!==s.id)continue;sites.push({id:s.id,name:s.name,type:s.type,distance:V.qual.distance(Math.hypot(cx-a.x,cy-a.y)),direction:dirName(cx-a.x,cy-a.y),condition:s.fire>0?'em chamas':s.integrity<55?'visivelmente danificado':'aparentemente íntegro'})}return sites.slice(0,10)}
function sensedInterior(a,range){const s=sim.currentSite?.(a);if(!s)return{containers:[],openings:[],room:null};const room=currentRoom(a),containers=(s.containers||[]).filter(c=>Math.hypot(c.x-a.x,c.y-a.y)<range*.55&&sim.world.rayClear(a.x,a.y,c.x,c.y,true)).map(c=>({id:c.id,name:c.name,room:c.room,distance:V.qual.distance(Math.hypot(c.x-a.x,c.y-a.y)),searched:!!c.searched,locked:!!c.locked})).slice(0,8),openings=[...(s.doors||[]),...(s.windows||[])].filter(o=>Math.hypot(o.x-a.x,o.y-a.y)<range*.65&&sim.world.rayClear(a.x,a.y,o.x,o.y,true)).map(o=>({id:o.id,type:o.openingType,distance:V.qual.distance(Math.hypot(o.x-a.x,o.y-a.y)),locked:!!o.locked,barricaded:(o.barricade||0)>0,open:!!o.open,integrity:o.integrity>65?'forte':o.integrity>30?'danificada':'quase cedendo'}));return{containers,openings,room:room?.name||null}}
sim.senseAgent=function(a,passive=false){V.ensurePerson(a);const weather=sim.world.weatherAt(sim.minute),state=clamp(1-(a.bodyClock?.sleepPressure||0)*.003-(a.pain||0)*.002-(a.stress||0)*.0012,.42,1),range=(175+(a.attr?.perception||4)*29+sim.skillLevel(a,'foraging')*4)*(.34+.66*lighting(a))*state*(1-weather.rain*.18),people=[],figures=[];for(const p of sim.peopleGrid.near(a.x,a.y,range)){if(p.id===a.id||!p.alive||!sim.canSee(a,p,range))continue;const known=a.socialModel?.has(p.id)||a.relations?.has(p.id)||Math.hypot(p.x-a.x,p.y-a.y)<65;people.push({id:p.id,name:known?p.name:'pessoa desconhecida',distance:V.qual.distance(Math.hypot(p.x-a.x,p.y-a.y)),direction:dirName(p.x-a.x,p.y-a.y),condition:sim.publicCondition(p),relationship:known?V.qual.relation(a.socialModel?.get(p.id)?.affinity??a.relations?.get(p.id)??0):'desconhecida',uniform:p.military?'uniforme militar':null})}
 const visible=sim.zombieGrid.near(a.x,a.y,range*1.06).filter(z=>z.alive&&sim.canSee(a,z,range*1.06)).slice(0,10),knew=!!a.knowledge?.zombieSeen;if(visible.length&&!passive)sim.recordZombieSight(a,visible[0],knew?'repeat':'sight');for(const z of visible)figures.push({id:z.id,distance:V.qual.distance(Math.hypot(z.x-a.x,z.y-a.y)),direction:dirName(z.x-a.x,z.y-a.y),appearance:knew?'figura humana agressiva com padrão já testemunhado':'pessoa com fe