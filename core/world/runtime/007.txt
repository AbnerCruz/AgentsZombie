ntGoal(z){let best=null,score=0;for(let oy=-4;oy<=4;oy++)for(let ox=-4;ox<=4;ox++){const q=sim.world.cellAtWorld(z.x+ox*W.CELL*2,z.y+oy*W.CELL*2),s=sim.scentMap.get(`${q.gx},${q.gy}`);if(!s)continue;const age=sim.minute-s.minute,val=s.strength*Math.exp(-age/26)-Math.hypot(q.x-z.x,q.y-z.y)/900;if(val>score){score=val;best=s}}return best}
sim.updateZombie=function(z,dt){if(!z.alive)return;if(Math.abs(z.knockVX||0)+Math.abs(z.knockVY||0)>.25){const km=sim.world.tryMove(z.x,z.y,(z.knockVX||0)*dt,(z.knockVY||0)*dt,5,null);if(km.moved){z.x=km.x;z.y=km.y}const decay=Math.pow(.18,dt);z.knockVX=(z.knockVX||0)*decay;z.knockVY=(z.knockVY||0)*decay}if(z.combat&&sim.minute<(z.combat.knockedUntil||0))return;z.mutation=sim.world.radiationAt(z.x,z.y,sim.minute)*.24;const close=sim.peopleGrid.near(z.x,z.y,390).filter(p=>p.alive&&!p.vehicleId).sort((a,b)=>AZ.dist2(z,a)-AZ.dist2(z,b)),target=close[0];let gx,gy;if(target){z.target=target;gx=target.x;gy=target.y;const d=Math.hypot(gx-z.x,gy-z.y);if(d<25&&sim.minute>=z.nextAttack){z.nextAttack=sim.minute+sim.rng.range(.5,1.6);sim.zombieWound(z,target);if(target.health<=0)sim.killPerson(target,'ataque de infectado');return}}else{let best=null,score=0;for(const n of sim.noiseEvents||[]){if(n.expires<=sim.minute)continue;const tr=sim.world.lineTransmission(z.x,z.y,n.x,n.y),d=Math.hypot(n.x-z.x,n.y-z.y),s=n.loudness*tr*700-d;if(s>score){score=s;best=n}}if(best&&score>35){gx=best.x;gy=best.y}else{const sc=scentGoal(z);if(sc){gx=sc.x;gy=sc.y}else{if(!z.target||z.target.alive===false||sim.rng.chance(.006*dt))z.target=sim.world.randomPointNear(z.x,z.y,480,Math.floor(sim.minute)+z.id.length);gx=z.target.x;gy=z.target.y}}}const flow=sim.world.flowDirection(z.x,z.y,gx,gy,sim.minute),l=Math.hypot(flow.x,flow.y)||1,speed=21+z.mutation*10,step=speed*dt,m=sim.world.tryMove(z.x,z.y,flow.x/l*step,flow.y/l*step,5,null);if(m.moved){z.x=m.x;z.y=m.y;z.facing=Math.atan2(flow.y,flow.x)}else{const q=sim.world.cellAtWorld(z.x+flow.x/l*10,z.y+flow.y/l*10);const op=sim.world.openingAtCell(q);if(op){const crowd=sim.zombieGrid.near(z.x,z.y,42).filter(x=>x.alive).length,damage=dt*(1.4+crowd*.22);sim.world.damageOpening(op,damage);if(sim.minute-(z._barrierNoiseAt||-999)>.7){z._barrierNoiseAt=sim.minute;sim.emitNoise?.(op.x,op.y,.38,'barrier_hit',z.id,'impactos contra uma barreira')}}else if(sim.rng.chance(.03*dt)){z.target=sim.world.randomPointNear(z.x,z.y,160,Math.floor(sim.minute)+z.id.length*3)}}};
// Consumo de comida estragada pode causar doença comum; não é a infecção dos mortos-vivos.
const consumeTool=sim.executeAgentTool.bind(sim);sim.executeAgentTool=function(p,name,args={},meta={}){if(name==='consume'&&args.item_id==='spoiled_food'&&sim.itemQty(p,'spoiled_food')){sim.takeItem(p,'spoiled_food',1);p.bodyClock.illness=clamp((p.bodyClock.illness||0)+.2+sim.rng.range(0,.25),0,1);p.stress=clamp(p.stress+8,0,100);sim.remember(p,'illness','Comeu alimento estragado e começou a se sentir mal.',6,['illness','food']);return}return consumeTool(p,name,args,meta)};
const v=document.querySelector('.drawerHead small');if(v)v.textContent='v0.9 · mundo físico + ocupação + pathfinding';
})();
