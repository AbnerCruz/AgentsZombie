tion.parameters.properties.opening_id={type:'string',description:'id de porta ou janela percebida quando a construção for uma barricada'};}
function addTool(name,description,props,req){if(AZ.AGENT_TOOLS.some(t=>t.function?.name===name))return;AZ.AGENT_TOOLS.push({type:'function',function:{name,description,parameters:{type:'object',properties:{...props,reason:{type:'string'}},required:[...req,'reason'],additionalProperties:false}}})}
addTool('open_barrier','Abrir uma porta ou janela percebida sem destruí-la.',{opening_id:{type:'string'}},['opening_id']);addTool('breach','Arrombar uma porta ou janela percebida usando força/ferramenta; faz bastante ruído.',{opening_id:{type:'string'},method:{type:'string',enum:['force','tool','weapon']}},['opening_id','method']);
if(oldApply)sim.applyBrainIntent=function(p,tool,args={},meta={}){if(tool==='open_barrier')return oldApply(p,'build',{structure:'open_barrier',opening_id:args.opening_id,reason:args.reason},meta);if(tool==='breach')return oldApply(p,'build',{structure:'breach',opening_id:args.opening_id,method:args.method,reason:args.reason},meta);return oldApply(p,tool,args,meta)};
sim.executeAgentTool=function(p,name,args={},meta={}){if(name==='scavenge'){const site=sim.findSiteById(args.site_id),co=args.container_id?sim.world.containerById(args.container_id):pickContainer(site,args.wanted);if(!co){sim.remember(p,'scavenge',site?`Não encontrou mais recipientes úteis em ${site.name}.`:'O local procurado não está acessível.',3,['scavenge']);markEvent(p,'resource_exhausted');return}return startSearch(p,co,args,meta.model)}if(name==='build'&&['open_barrier','breach'].includes(args.structure)){const op=sim.world.openingById(args.opening_id);if(!op){markEvent(p,'barrier_invalid');return}if(approach(p,'build',args,op,meta.model,20))return;if(args.structure==='open_barrier'){if(!op.locked&&(op.barricade||0)<=0){sim.world.setOpening(op,true);sim.emitNoise?.(op.x,op.y,.08,'door',p.id,'porta abrindo');sim.remember(p,'barrier','Abriu uma passagem.',3,['barrier',op.id])}else{sim.remember(p,'barrier','A passagem está trancada ou barricada.',5,['barrier',op.id]);markEvent(p,'barrier_locked')}return}const tool=sim.weaponStats(p),dur=Math.max(1.5,7-(p.attr.strength||4)*.35-sim.skillLevel(p,'mechanics')*.25);p.action={tool:'breach_v9',args:{...args},started:sim.minute,duration:dur,progress:0,model:meta.model||'brain'};sim.emitNoise?.(op.x,op.y,.72,'breach',p.id,'impactos contra uma barreira');return}if(name==='build'&&args.structure==='barricade'&&args.opening_id){const op=sim.world.openingById(args.opening_id);if(!op)return;if(approach(p,'build',args,op,meta.model,20))return;if(sim.itemQty(p,'wood')<2){markEvent(p,'missing_material');return}p.action={tool:'barricade_v9',args:{...args},started:sim.minute,duration:26,progress:0,model:meta.model||'brain'};return}if(name==='build'&&['wall','trap','well'].includes(args.structure)){const needs=args.structure==='wall'?{wood:3,metal:1}:args.structure==='trap'?{wood:2,metal:1}:{wood:4,metal:2};if(!sim.hasNeed(p,needs)){markEvent(p,'missing_material');return}p.action={tool:'construction_v9',args:{...args,needs},started:sim.minute,duration:args.structure==='well'?140:args.structure==='wall'?65:38,progress:0,model:meta.model||'brain'};return}return oldExec(p,name,args,meta)};
sim.finishAction=function(p,a){
 if(a.tool==='unlock_container_v9'){const co=sim.world.containerById(