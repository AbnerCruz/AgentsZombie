.lx]=O.VEHICLE;v.occ={gx:nq.gx,gy:nq.gy};v.angle=Math.atan2(dy,dx);v.fuel=Math.max(0,v.fuel-step/1450);v.dirty=true;p.x=v.x;p.y=v.y;sim.practice(p,'driving',step/120);if(sim.minute-(nav.lastEngineNoise||-999)>.35){nav.lastEngineNoise=sim.minute;sim.emitNoise?.(v.x,v.y,.72,'engine',p.id,'motor de veículo')}for(const z of sim.zombieGrid.near(v.x,v.y,22)){if(!z.alive)continue;const dmg=20+speed*.08;z.health-=dmg;z.knockVX=(dx/d)*Math.min(42,10+dmg*.7);z.knockVY=(dy/d)*Math.min(42,10+dmg*.7);v.condition=Math.max(0,v.condition-dmg*.03);if(z.health<=0){z.alive=false;sim.creditKill?.(p,z,'vehicle')}}};
function pickContainer(site,wanted=''){if(!site)return null;const w=String(wanted).toLowerCase(),score=co=>{refreshContainer(co);let s=co.searched?-4:2;if(/med|curativ|remédio|remedio/.test(w)&&co.type==='medicine')s+=5;if(/arma|muni/.test(w)&&co.type==='armory')s+=6;if(/comida|água|agua/.test(w)&&['kitchen','shelf','farm'].includes(co.type))s+=5;if(/gas|combust/.test(w)&&['fuel','garage'].includes(co.type))s+=5;return s+((co.loot||[]).some(i=>i.qty>0)?1:0)};return(site.containers||[]).filter(co=>(co.loot||[]).some(i=>i.qty>0)).sort((a,b)=>score(b)-score(a))[0]||null}
function approach(p,tool,args,e,model='brain',radius=24){const d=Math.hypot(e.x-p.x,e.y-p.y);if(d<=radius)return false;p.target={x:e.x,y:e.y,id:e.id};p.action={tool,args:{...args,_phase:'approach'},started:sim.minute,duration:Math.max(1,d/44),progress:0,model};return true}
function startSearch(p,co,args={},model='brain'){if(!co)return false;if(approach(p,'scavenge',{site_id:co.siteId,wanted:args.wanted||'',container_id:co.id,reason:args.reason||'revistar recipiente'},co,model,22))return true;refreshContainer(co);if(co.locked){p.action={tool:'unlock_container_v9',args:{...args,container_id:co.id},started:sim.minute,duration:Math.max(3,8-sim.skillLevel(p,'mechanics')*.35),progress:0,model};sim.emitNoise?.(p.x,p.y,.32,'lock_work',p.id,'tentativa de abrir um recipiente trancado');return true}const dur=Math.max(3,8-sim.skillLevel(p,'foraging')*.35);p.action={tool:'search_container_v9',args:{...args,container_id:co.id},started:sim.minute,duration:dur,progress:0,model};sim.emitNoise?.(p.x,p.y,.13,'search',p.id,'objetos sendo remexidos');return true}
function transferFromContainer(p,co,wanted=''){refreshContainer(co);const options=(co.loot||[]).filter(i=>i.qty>0),needle=String(wanted||'').toLowerCase();options.sort((a,b)=>((b.name.toLowerCase().includes(needle)||b.id.includes(needle))?2:0)-((a.name.toLowerCase().includes(needle)||a.id.includes(needle))?2:0));let moved=0;for(const i of options){if(moved>=3)break;const nextW=sim.inventoryWeight(p)+(i.weight||.5),nextV=sim.inventoryVolume(p)+W.itemVolume(i);if(nextW>sim.maxCarry(p)||nextV>sim.maxVolume(p))continue;i.qty--;co.dirty=true;sim.addItem(p,i,1);moved++;sim.world.regionStats.itemsRemoved++}co.searched=true;co.dirty=true;sim.world.regionStats.containersSearched++;sim._v9.containerSearches++;sim.practice(p,'foraging',2+moved);sim.remember(p,'scavenge',moved?`Revistou ${co.name} e levou ${moved} item(ns).`:`Revistou ${co.name}, mas não encontrou nada que pudesse levar.`,moved?5:3,['scavenge',co.id]);return moved}
const buildTool=AZ.AGENT_TOOLS?.find(t=>t.function?.name==='build');if(buildTool){const en=buildTool.function.parameters.properties.structure.enum;for(const x of ['wall','trap','well'])if(!en.includes(x))en.push(x);buildTool.func