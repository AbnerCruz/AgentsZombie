sim.resolveTalk=function(a,b,message,intent){if(!a?.alive||!b?.alive)return;ensure(a);ensure(b);const ab=axis(a,b),ba=axis(b,a);ab.familiarity=clamp(ab.familiarity+1,0,100);ba.familiarity=clamp(ba.familiarity+1,0,100);sim.emitNoise?.(a.x,a.y,.12,'voice',a.id,'conversa');sim.remember(a,'dialogue_intent',`Tentou conversar com ${b.name}: “${message}”`,4,['social',b.id]);S.requestScene(a,b,{message:String(message||''),intent:String(intent||'conversation')},'talk')};
sim.resolveTrade=function(a,b,args){if(!a?.alive||!b?.alive)return;ensure(a);ensure(b);const oq=Math.max(0,args.offer_qty|0),rq=Math.max(0,args.request_qty|0);if(oq&&sim.itemQty(a,args.offer_item)<oq){sim.remember(a,'trade','A oferta deixou de estar disponível.',3,['trade']);return}if(rq&&sim.itemQty(b,args.request_item)<rq){sim.remember(a,'trade',`${b.name} já não possui o item pedido.`,3,['trade']);return}S.requestScene(a,b,{intent:'trade',message:args.message||'',proposal:{offer_item:args.offer_item,offer_qty:oq,request_item:args.request_item,request_qty:rq,offer_value:localValue(b,args.offer_item,oq),request_value:localValue(b,args.request_item,rq)}},'trade')};
function promiseTick(){for(const p of sim.people){if(!p.alive)continue;ensure(p);for(const pr of p.promises){if(pr.from!==p.id||pr.status!=='pending'||sim.minute<pr.due)continue;const to=sim.entityById(pr.to);if(!to){pr.status='expired';continue}const recent=(p.memory?.entries||[]).filter(m=>m.minute>=pr.created&&m.tags?.includes(to.id));const fulfilled=pr.type==='share_resource'||pr.type==='repay'?recent.some(m=>m.kind==='trade'||m.kind==='aid'):pr.type==='help'?recent.some(m=>m.kind==='aid'||m.kind==='combat'):false;if(fulfilled){pr.status='fulfilled';axis(to,p).trust=clamp(axis(to,p).trust+8,-100,100);axis(to,p).respect=clamp(axis(to,p).respect+5,-100,100)}else if(pr.type!=='other'&&pr.type!=='information'){pr.status='broken';axis(to,p).trust=clamp(axis(to,p).trust-18,-100,100);axis(to,p).affinity=clamp(axis(to,p).affinity-6,-100,100);V.learnFact(to,`reputation:${p.id}:promise`,`${p.name} não cumpriu uma promessa importante.`,to.id,.9,{targetId:p.id,reputation:true});V.markReplan?.(to,'promise_broken',true)}}if(p.promises.length>48)p.promises=p.promises.filter(pr=>pr.status==='pending'||sim.minute-(pr.due||pr.created||sim.minute)<1440)}}
function socialObservation(){if(sim.paused)return;const now=sim.minute,delta=Math.max(0,Math.min(30,now-(S.lastSocialMinute??now)));S.lastSocialMinute=now;if(delta<=0)return;const alive=sim.people.filter(p=>p.alive),bySite=new Map();for(const p of alive){ensure(p);bySite.set(p.id,sim.currentSite?.(p)?.id||null)}for(const a of alive){for(const b of sim.peopleGrid.near(a.x,a.y,120)){if(!b.alive||a===b||String(a.id)>=String(b.id))continue;ensure(b);const sameSite=bySite.get(a.id)&&bySite.get(a.id)===bySite.get(b.id),known=a.socialModel?.has(b.id)||b.socialModel?.has(a.id);if(!sameSite&&!known&&!a.equippedWeapon&&!b.equippedWeapon)continue;let ab=a.socialModel?.get(b.id),ba=b.socialModel?.get(a.id);if(sameSite){ab=ab||axis(a,b);ba=ba||axis(b,a);for(const s of[ab,ba]){s.sharedTime=clamp((s.sharedTime||0)+delta,0,500);s.familiarity=clamp((s.familiarity||0)+delta*.025,0,100)}if(a.action?.tool==='rest'&&b.action?.tool==='rest'){ab.trust=clamp(ab.trust+delta*.012,-100,100);ba.trust=clamp(ba.trust+delta*.012,-100,100)}}if(b.equippedWeapon&&sim.canSee?.(a,b,120)){ab=ab||axis(a,b);ab.fear=clamp(ab.fear+delta*.004,0,100);if(sim.skillLevel(b,'melee')+sim.skillLevel(b,'shooting')>7)ab.respect=clamp((ab.respect||0)+delta*.003,-100,100)}if(a.equippedWeapon&&sim.canSee?.(b,a,120)){ba=ba||axis(b,a);ba.fear=clamp(ba.fear+delta*.004,0,100);if(sim.skillLevel(a,'melee')+sim.skillLevel(a,'shooting')>7)ba.respect=clamp((ba.respect||0)+delta*.003,-100,100)}}}}
