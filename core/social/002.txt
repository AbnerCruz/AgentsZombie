        reputation:{type:'array',items:{type:'object',additionalProperties:false,required:['listener_id','target_id','claim','confidence'],properties:{listener_id:{type:'string'},target_id:{type:'string'},claim:{type:'string'},confidence:{type:'number',minimum:.05,maximum:.98}}}}
      }
    }
  };
}
function parseJSON(txt){try{return JSON.parse(txt)}catch{}try{return JSON.parse(String(txt).replace(/^```json\s*/i,'').replace(/```\s*$/,''))}catch{return null}}
function rememberScene(parts,out){const lines=(out.turns||[]).map(t=>{const p=parts.find(x=>x.id===t.speaker_id);return`${p?.name||t.speaker_id}: “${t.text}”`});const summary=out.outcome?.summary||lines.join(' / ');for(const p of parts){sim.remember(p,'dialogue',summary,7,['social','scene',...parts.filter(q=>q!==p).map(q=>q.id)]);p.brain.novelty=(p.brain.novelty||0)+2;V.markReplan?.(p,'social_scene_resolved',out.outcome?.type==='rupture'||out.outcome?.type==='threat')}}
function applyRelations(parts,out){for(const d of out.relation_changes||[]){const a=parts.find(x=>x.id===d.from_id),b=parts.find(x=>x.id===d.to_id);if(!a||!b||a===b)continue;const s=axis(a,b);for(const k of ['affinity','trust','debt','respect'])s[k]=clamp((s[k]||0)+(+d[k]||0),-100,100);s.fear=clamp((s.fear||0)+(+d.fear||0),0,100);s.familiarity=clamp((s.familiarity||0)+Math.max(0,+d.familiarity||0),0,100);s.lastInteraction=sim.minute;s.cooperation=clamp((s.cooperation||0)+(out.outcome?.type==='agreement'||out.outcome?.type==='alliance'?3:out.outcome?.type==='rupture'?-4:0),-100,100)}}
function applyBeliefs(parts,out){for(const b of out.belief_updates||[]){const p=parts.find(x=>x.id===b.listener_id);if(!p)continue;const src=parts.find(x=>x.id===b.source_id),trust=src?axis(p,src).trust:0,conf=clamp((+b.confidence||.5)*(.72+(trust+100)/500),.05,.96);V.learnFact(p,b.key||V.topicKey(b.content),b.content,b.source_id||'conversation',conf);if(src)axis(p,src).knownBeliefs.add(b.key||V.topicKey(b.content))}for(const d of out.deceptions||[]){for(const id of d.listeners||[]){const p=parts.find(x=>x.id===id),src=parts.find(x=>x.id===d.speaker_id);if(!p||!src)continue;const conf=clamp(.42+(axis(p,src).trust+100)/500,.15,.82);V.learnFact(p,V.topicKey(d.claim),d.claim,src.id,conf,{possiblyFalse:true})}}for(const r of out.reputation||[]){const p=parts.find(x=>x.id===r.listener_id),t=parts.find(x=>x.id===r.target_id)||sim.entityById(r.target_id);if(!p||!t)continue;const key=`reputation:${t.id}:${V.topicKey(r.claim)}`;V.learnFact(p,key,r.claim,'gossip',r.confidence,{targetId:t.id,reputation:true});p.reputation.set(t.id,{claim:r.claim,confidence:r.confidence,minute:sim.minute})}}
