ai.request=async function(job){const a=job.agent,model=job.deep?this.deepModel:this.lightModel,tools=this.selectTools(job.trigger,a),sys=`Você é o cérebro individual de ${a.name}. Você escolhe UMA intenção de alto nível, não micro-passos. O corpo executa rotinas, navegação, colisão e reflexos localmente. Use somente sensores, memórias, crenças, relações e lugares fornecidos. Não use conhecimento de gênero ou bastidores. Não invente resultados físicos. reason deve ser curto, em primeira pessoa. Se a situação não mudou materialmente, preserve o plano em vez de criar comportamento aleatório.`;let err=null;for(let attempt=0;attempt<2;attempt++){try{const ctx=this.buildContext(a,job.trigger),sp=splitContext(ctx),body={model,messages:[{role:'system',content:sys},{role:'user',content:'ESTÁVEL\n'+JSON.stringify(sp.stable)},{role:'user',content:'MEMÓRIA E MODELO SOCIAL\n'+JSON.stringify(sp.memory)},{role:'user',content:'AGORA\n'+JSON.stringify(sp.current)}],tools,tool_choice:'required'};if(job.deep)body.reasoning={effort:'medium'};const data=await this.fetchCompletion(body,a.id);this.calls++;if(attempt)this.fallbacks++;this.accountUsage(data.usage,model,a);const msg=data.choices?.[0]?.message,tc=msg?.tool_calls?.[0];if(!tc?.function?.name)throw new Error('Modelo não retornou intenção executável.');let args={};try{args=JSON.parse(tc.function.arguments||'{}')}catch{throw new Error('Argumentos inválidos.')}a.brain.lastThought=String(args.reason||msg?.content||'').trim()||'Preciso ajustar meu plano.';a.brain.lastModel=model;a.brain.lastDecisionAt=sim.minute;sim.applyBrainIntent?.(a,tc.function.name,args,{deep:job.deep,event:job.event||{reason:job.trigger,urgent:false},model});sim.replayLog=sim.replayLog||[];sim.replayLog.push({minute:sim.minute,agent:a.id,tool:tc.function.name,args:{...args,reason:undefined},reason:a.brain.lastThought,schema_tools:tools.length});if(sim.replayLog.length>5000)sim.replayLog.splice(0,sim.replayLog.length-5000);success(a);return true}catch(e){err=e;if(![408,409,429,500,502,503,504].includes(e?.status)||attempt)break;await new Promise(r=>setTimeout(r,350+Math.random()*650))}}fail(a,err);return false};
