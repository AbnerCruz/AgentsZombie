E.relevance=function(agent,event){let score=0;const r=String(event?.reason||'');if(event?.urgent)score+=6;if(/death|threat|wound|promise_broken|rupture|betray|group_decision|first_/.test(r))score+=4;if(agent.health<35||agent.infection>.35||agent.bleeding>1.5)score+=3;const cam=sim.camera?Math.hypot(agent.x-sim.camera.x,agent.y-sim.camera.y):9999;if(cam<700)score+=2;score+=Math.max(0,2-(agent.lodOffset||0));return score};
E.allow=function(agent,event){if(!E.guard.enabled)return true;const m=E.metrics(),callCap=E.guard.maxCallsPerDay,costCap=E.guard.maxCostPerDay;if(callCap>0&&m.callsDay>=callCap)return false;if(costCap>0&&m.costDay>=costCap)return false;const ratio=Math.max(callCap>0?m.callsDay/callCap:0,costCap>0?m.costDay/costCap:0),rel=E.relevance(agent,event);if(ratio>.95&&rel<7)return false;if(ratio>.8&&rel<4)return false;return true};
