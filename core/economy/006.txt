ai.decide=function(agent,deep=false,event={reason:'replan',urgent:false}){if(!E.allow(agent,event)){agent.brain.replanPending=null;agent.brain.lastError='Proteção opcional de gasto atingiu o limite configurado.';return false}const hash=situationHash(agent,event),last=agent.brain.lastSituationHash;if(!event.urgent&&last===hash&&sim.minute-(agent.brain.lastSituationAt||0)<18){agent.brain.replanPending=null;agent.brain.deduped=(agent.brain.deduped||0)+1;return false}const started=baseDecide(agent,significant(agent,event),event);if(started){agent.brain.lastSituationHash=hash;agent.brain.lastSituationAt=sim.minute}return started};
function retryDelay(n){return Math.min(30000,1200*Math.pow(1.8,Math.max(0,(n||1)-1)))}
function fail(a,e){const msg=String(e?.message||e||'Falha desconhecida'),t=performance.now();ai.fails++;ai.lastError=msg;a.brain.lastError=msg;a.brain.failStreak=(a.brain.failStreak||0)+1;a.brain.lastFailureAt=t;a.brain.retryAfter=t+retryDelay(a.brain.failStreak);sim.remember(a,'system',`O cérebro externo não conseguiu reavaliar a situação: ${msg}`,2,['ai-error'])}
