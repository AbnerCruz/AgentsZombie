ai.accountUsage=function(u={},model,agent){const before=this.cost||0;baseUsage(u,model,agent);const cost=Math.max(0,(this.cost||0)-before),total=u.total_tokens||((u.prompt_tokens||0)+(u.completion_tokens||0));E.events.push({real:Date.now(),minute:sim.minute,day:sim.day,cost,tokens:total,model,agent:agent?.id||null});if(E.events.length>2400)E.events.splice(0,E.events.length-2400)};
function dayEvents(day=sim.day){return E.events.filter(x=>x.day===day)}
E.metrics=function(){const now=Date.now(),recent=E.events.filter(x=>now-x.real<=300000),d=dayEvents(),simRecent=E.events.filter(x=>sim.minute-x.minute<=60),cost5=recent.reduce((n,x)=>n+x.cost,0),costDay=d.reduce((n,x)=>n+x.cost,0),callsDay=d.length,projectCalls=simRecent.length?Math.round(simRecent.length*24):callsDay,projectCost=simRecent.length?simRecent.reduce((n,x)=>n+x.cost,0)*24:costDay;return{callsDay,costDay,costPerHour:cost5*12,projectCallsPerSimDay:projectCalls,projectCostPerSimDay:projectCost,softTarget:E.softTarget,alert:callsDay>=10&&(projectCalls>E.softTarget*2||projectCost>Math.max(.08,costDay*3))}};
