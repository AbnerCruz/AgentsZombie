next)telemetry.innerHTML=next}
window.addEventListener('az-selection',renderTelemetry);setInterval(renderTelemetry,650);renderTelemetry();
const valid=new Set(['person','zombie','site','vehicle','structure']);function normalize(o){if(!o)return null;if(!o.kind){if(Number.isFinite(o.w)&&Number.isFinite(o.h)&&Array.isArray(o.loot))o.kind='site';else if(Number.isFinite(o.maxFuel)&&Array.isArray(o.trunk))o.kind='vehicle';else if(Number.isFinite(o.integrity)||o.created!=null||o.owner!=null)o.kind='structure'}return valid.has(o.kind)?o:null}for(const s of sim.structures||[])normalize(s);const oldPick=renderer.pick.bind(renderer),oldCenter=renderer.centerOnSelected.bind(renderer),oldRender=renderer.render.bind(renderer);renderer.pick=function(x,y){this.follow=false;oldPick(x,y);this.sim.selected=normalize(this.sim.selected);if(!this.sim.selected)this.follow=false};renderer.centerOnSelected=function(){this.sim.selected=normalize(this.sim.selected);if(!this.sim.selected){this.follow=false;return}return oldCenter()};renderer.render=function(){if(this.follow&&!normalize(this.sim.selected))this.follow=false;try{return oldRender()}catch(e){console.error('Renderer recovered',e);this.follow=false;this.sim.selected=null;this.pointer.down=false;this.pointer.moved=false;this.touches?.clear?.();return oldRender()}};const resetPointer=()=>{renderer.pointer.down=false;renderer.pointer.moved=false;renderer.touches?.clear?.();renderer.pinch=null};renderer.canvas.addEventListener('pointerdown',()=>{renderer.follow=false},{capture:true});renderer.canvas.addEventListener('lostpointercapture',resetPointer);window.addEventListener('blur',resetPointer);document.addEventListener('visibilitychange',()=>{if(document.hidden)resetPointer()});
const oldDP=renderer.drawPerson.bind(renderer),oldDZ=renderer.drawZombie.bind(renderer),oldDE=renderer.drawEffects.bind(renderer);renderer.drawPerson=function(p){oldDP(p);if((p.hitFlashUntil||0)>performance.now()){const c=this.ctx;c.save();c.globalAlpha=.6;c.fillStyle='#fff2e8';c.fillRect(p.x-7,p.y-11,14,24);c.restore()}};renderer.drawZombie=function(z){oldDZ(z);if((z.hitFlashUntil||0)>performance.now()){const c=this.ctx;c.save();c.globalAlpha=.65;c.fillStyle='#ffd4c7';c.fillRect(z.x-7,z.y-11,14,23);c.restore()}};renderer.drawEffects=function(){oldDE();const c=this.ctx;for(const e of sim.effects)if(e.type==='impact_v08'){const r=Math.max(3,14-e.life);c.strokeStyle='rgba(255,220,160,.9)';c.lineWidth=1.5;c.beginPath();c.arc(e.x,e.y,r,0,Math.PI*2);c.stroke();if(e.amount){c.fillStyle='rgba(255,240,225,.9)';c.font='8px system-ui';c.textAlign='center';c.fillText(`-${e.amount}`,e.x,e.y-11)}}};
V.uiReady=true;
})();
