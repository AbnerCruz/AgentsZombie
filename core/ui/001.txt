Model').value=sim.ai.deepModel;if($('aiEnabled'))$('aiEnabled').checked=sim.ai.enabled;
function save(){sim.ai.saveSettings($('apiKey')?.value,$('managementKey')?.value,$('lightModel')?.value,$('deepModel')?.value,$('aiEnabled')?.checked);for(const p of sim.people)if(V.isAgent(p)&&p.alive)V.markReplan(p,'ai_configuration_changed',false);syncMetrics()}
if($('saveKey'))$('saveKey').onclick=save;if($('clearKey'))$('clearKey').onclick=()=>{sim.ai.saveSettings('','',$('lightModel')?.value,$('deepModel')?.value,$('aiEnabled')?.checked);$('apiKey').value='';$('managementKey').value='';renderMetrics()};if($('aiEnabled'))$('aiEnabled').onchange=save;
const money=v=>Number.isFinite(+v)?`US$ ${(+v).toFixed(6).replace(/0+$/,'').replace(/\.$/,'')}`:'—',compact=v=>Intl.NumberFormat('pt-BR',{notation:'compact'}).format(v||0);
function renderMetrics(){if(!$('aiMetricGrid'))return;const cr=sim.ai.account?.credits,k=sim.ai.account?.key,bal=cr?Number(cr.total_credits||0)-Number(cr.total_usage||0):NaN;$('orBalance').textContent=Number.isFinite(bal)?money(bal):'—';$('aiMetricGrid').innerHTML=[['Custo sessão',money(sim.ai.cost)],['Requests',sim.ai.requests],['Sucessos',sim.ai.calls],['Falhas',sim.ai.fails],['Retries',sim.ai.fallbacks],['Input',compact(sim.ai.promptTokens)],['Output',compact(sim.ai.completionTokens)],['Reasoning',compact(sim.ai.reasoningTokens)],['Cached',compact(sim.ai.cachedTokens)],['Latência',`${Math.round(sim.ai.totalLatency/Math.max(1,sim.ai.requests))} ms`],['Uso key',k?money(k.usage):'—'],['Uso hoje',k?money(k.usage_daily):'—'],['Uso mensal',k?money(k.usage_monthly):'—'],['Limite key',k?(k.limit==null?'sem limite':money(k.limit)):'—']].map(x=>`<div><span>${x[0]}</span><b>${x[1]}</b></div>`).join('');$('aiByModel').innerHTML=Object.entries(sim.ai.byModel).sort((a,b)=>b[1].cost-a[1].cost).map(([n,v])=>`<div class="v8row"><span>${esc(n)}</span><b>${v.calls} · ${compact(v.tokens)} tok · ${money(v.cost)}</b></div>`).join('')||'<span class="hint">Sem uso.</span>';$('aiByAgent').innerHTML=Object.values(sim.ai.byAgent).sort((a,b)=>b.cost-a.cost).map(v=>`<div class="v8row"><span>${esc(v.name)}</span><b>${v.calls} · ${compact(v.tokens)} tok · ${money(v.cost)}</b></div>`).join('')||'<span class="hint">Sem uso.</span>'}
let syncBusy=false;async function syncMetrics(){if(syncBusy||(!sim.ai.apiKey&&!sim.ai.managementKey))return;syncBusy=true;const st=$('aiMetricsStatus');if(st)st.textContent='sincronizando automaticamente...';try{const r=await sim.ai.refreshAccountMetrics();if(st)st.textContent=r.error?`erro: ${r.error}`:`automático · ${new Date(r.lastSync).toLocaleTimeString()}`}finally{syncBusy=false;renderMetrics()}}
setTimeout(syncMetrics,1600);setInterval(syncMetrics,45000);document.addEventListener('visibilitychange',()=>{if(!document.hidden&&Date.now()-(sim.ai.account?.lastSync||0)>30000)syncMetrics()});setInterval(renderMetrics,1200);
function renderRanking(){const root=$('killRanking');if(!root)return;const rows=sim.people.slice().sort((a,b)=>(b.stats?.kills||0)-(a.stats?.kills||0)||(b.stats?.zombieKills||0)-(a.stats?.zombieKills||0)).slice(0,50);root.innerHTML=rows.map((p,i)=>`<button class="rankRow" data-person="${esc(p.id)}"><b>#${i+1}</b><span>${esc(p.name)}<small>${p.alive?'vivo':'morto'} · ${p.type==='agent'?'agente IA':p.military?'militar':'NPC'}</small></span><b>${p.stats?.kills||0}<small>${p.stats?.zombieKills||0} ameaças · ${p.stats?.humanKills||0} humanos</small></b></button>`).join('');root.querySelectorAll('[data-person]').forEach(el=>el.onclick=()=>{const p=sim.people.find(x=>x.id===el.dataset.person);if(!p)return;sim.selec