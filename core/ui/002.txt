ted=p;renderer.centerOnSelected();window.dispatchEvent(new CustomEvent('az-selection',{detail:p}));document.querySelector('[data-tab="inspect"]')?.click()})}
setInterval(()=>{if(document.querySelector('[data-tab="ranking"]')?.classList.contains('active'))renderRanking()},900);
const inspector=$('inspector');let telemetry=$('stableAgentTelemetry');if(inspector&&!telemetry){telemetry=document.createElement('div');telemetry.id='stableAgentTelemetry';telemetry.className='v8telemetry hidden';inspector.insertAdjacentElement('afterend',telemetry)}
function layer(p){if(p.combat?.grabbedBy&&sim.minute<p.combat.grabbedUntil)return'Reflexo';if(p.brain?.pending)return p.brain?.identityDueDay?'Identidade':'Plano';if(p.action?.args?._localRoutine)return'Rotina';return'Rotina'}
function intentHTML(i){return i?`<b>${esc(i.tool==='bridge_move'?'exploração local':i.tool)}</b><small>${esc(i.reason||'sem descrição')}${i.bridge?' · zero token':''}</small>`:'<small>nenhuma</small>'}
function renderTelemetry(){if(!telemetry)return;const p=sim.selected;if(!p||p.kind!=='person'){telemetry.classList.add('hidden');return}V.ensurePerson(p);telemetry.classList.remove('hidden');const s=sim.observeSense(p),vision=[...(s.vision?.people||[]),...(s.vision?.unidentified_figures||[])].slice(0,8),hearing=s.hearing||[],beliefs=[...p.beliefs.values()].sort((a,b)=>b.confidence-a.confidence).slice(0,7),social=[...p.socialModel.entries()].map(([id,x])=>({q:sim.entityById(id),x})).filter(z=>z.q).sort((a,b)=>b.x.familiarity-a.x.familiarity).slice(0,5),b=p.brain||{},bc=p.bodyClock||{};const next=`<h3>Cognição</h3><div class="v8grid"><div class="v8box"><b>CAMADA ATIVA · ${layer(p)}</b><small>${b.pending?'reavaliando mudança sem interromper o corpo':'execução local sem rede'}</small></div><div class="v8box"><b>PLANO</b><small>${esc(b.plan?.summary||'ainda não consolidado')}</small></div><div class="v8box"><b>INTENÇÃO ATUAL</b>${intentHTML(b.intent)}</div><div class="v8box"><b>PRÓXIMA INTENÇÃO</b>${intentHTML(b.nextIntent)}</div><div class="v8box"><b>RITMO CORPORAL</b><small>pressão de sono ${Math.round(bc.sleepPressure||0)}% · mobilidade ${Math.round((bc.mobility||1)*100)}%${bc.illness>0?` · doença ${Math.round(bc.illness*100)}%`:''}</small></div></div><h3>Sensores</h3><div class="v8grid"><div class="v8box"><b>VISÃO · ${esc(s.vision?.facing||'—')}</b><small>${vision.length?vision.map(v=>`${esc(v.name||v.appearance||v.recognition||'figura')} · ${esc(v.direction)} · ${esc(v.distance)}`).join('<br>'):'nada relevante no campo visual'}</small></div><div class="v8box"><b>AUDIÇÃO</b><small>${hearing.length?hearing.map(h=>`${esc(h.sound)} · ${esc(h.direction)} · ${esc(h.distance)} · ${esc(h.intensity)}`).join('<br>'):'nenhum som relevante'}</small></div><div class="v8box"><b>RUÍDO PRÓPRIO</b><small>${esc(s.own_noise||'—')}</small></div></div><h3>Crenças</h3><div class="v8grid">${beliefs.length?beliefs.map(x=>`<div class="v8box"><b>${esc(V.qual.confidence(x.confidence))}</b><small>${esc(x.content)} · fonte ${esc(x.firsthand?'experiência direta':x.source)}</small></div>`).join(''):'<div class="v8box"><small>Nenhuma crença consolidada.</small></div>'}</div><h3>Relações multidimensionais</h3><div class="v8grid">${social.length?social.map(({q,x})=>`<div class="v8box"><b>${esc(q.name)}</b><small>afinidade ${esc(V.qual.relation(x.affinity))} · confiança ${esc(V.qual.relation(x.trust))} · medo ${esc(V.qual.need(x.fear))} · familiaridade ${esc(V.qual.need(x.familiarity))}</small></div>`).join(''):'<div class="v8box"><small>Sem relações consolidadas.</small></div>'}</div>`;if(telemetry.innerHTML!==next