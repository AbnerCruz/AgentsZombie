pses(){let added=false;for(const c of sim.corpses)if(!c.reanimated&&!corpseKnown.has(c.id)){corpseKnown.add(c.id);corpseQueue.push(c);added=true}if(added)corpseQueue.sort((a,b)=>a.reanimateAt-b.reanimateAt)}
ingestCorpses();sim.updateCorpses=function(){ingestCorpses();let changed=false;while(corpseQueue.length&&corpseQueue[0].reanimateAt<=sim.minute){const c=corpseQueue.shift();if(c.reanimated)continue;c.reanimated=true;sim.zombies.push(sim.makeZombie(c.x,c.y,{name:c.name}));sim.log('INFECÇÃO',`${c.name} reanimou após a morte.`);changed=true}if(changed){sim.corpses=sim.corpses.filter(c=>!c.reanimated);corpseKnown=new Set(sim.corpses.map(c=>c.id))}};

// Render: culling sem leitura de layout por entidade.
if(renderer){renderer.inView=function(x,y,pad){const hw=(this.canvas.width/this.dpr)/(2*this.sim.camera.zoom)+pad,hh=(this.canvas.height/this.dpr)/(2*this.sim.camera.zoom)+pad;return Math.abs(x-this.sim.camera.x)<hw&&Math.abs(y-this.sim.camera.y)<hh};renderer.drawStructures=function(){const ctx=this.ctx;for(const s of this.sim.structures){if(!this.inView(s.x,s.y,70))continue;ctx.fillStyle=s.type==='campfire'?'#ef8b42':s.type==='checkpoint'?'#7c806f':'#a58a61';if(s.type==='checkpoint'){ctx.fillRect(s.x-24,s.y-5,48,10);ctx.fillStyle='#d1c9a7';ctx.fillRect(s.x-19,s.y-3,10,6);ctx.fillRect(s.x+8,s.y-3,10,6)}else ctx.fillRect(s.x-6,s.y-6,12,12)}};renderer.drawCorpses=function(){const ctx=this.ctx;ctx.fillStyle='#5e3d3a';for(const c of this.sim.corpses){if(c.reanimated||!this.inView(c.x,c.y,50))continue;ctx.fillRect(c.x-8,c.y-3,16,6);ctx.fillStyle='#2c2524';ctx.fillRect(c.x+5,c.y-5,5,5);ctx.fillStyle='#5e3d3a'}}}

// Limpeza leve e métricas de diagnóstico, sem trabalho pesado por frame.
const oldRebuild=sim.rebuildSpatial.bind(sim);sim.rebuildSpatial=function(){oldRebuild();pruneNoise();if(world.edgeCache?.size>1200){let n=world.edgeCache.size-1000;for(const k of world.edgeCache.keys()){world.edgeCache.delete(k);if(--n<=0)break}}};
const econ=sim.ai?.economy;if(econ?.metrics){const rawMetrics=econ.metrics.bind(econ);let metricAt=0,metricCache=null;econ.metrics=function(){const now=performance.now();if(metricCache&&now-metricAt<750)return metricCache;metricAt=now;return metricCache=rawMetrics()}}
perf.snapshot=()=>({gridMoves:perf.gridMoves,sensorBuilds:perf.sensorBuilds,zombieSensePulses:perf.zombieSensePulses,noiseQueries:perf.noiseQueries,noiseEvents:sim.noiseEvents.length,scentCells:sim.scentMap.size,entityCache:entityIndex.size,flowFields:world.flowCache?.size||0,edgeCache:world.edgeCache?.size||0,chunks:world.chunks.size,people:sim.people.length,zombies:sim.zombies.length,corpses:sim.corpses.length});
})();
