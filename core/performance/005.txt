onst out=[];for(const p of sim.peopleGrid.near(a.x,a.y,260)){if(p.id===a.id||!p.alive)continue;const s=a.socialModel?.get(p.id),legacy=a.relations?.get(p.id),known=!!s||legacy!=null;out.push({id:p.id,name:known?p.name:'pessoa desconhecida',affinity:s?V.qual.relation(s.affinity):legacy!=null?V.qual.relation(legacy):'desconhecida',trust:s?V.qual.relation(s.trust):'desconhecida',fear:s?V.qual.need(s.fear):'desconhecido',familiarity:s?V.qual.need(s.familiarity):'desconhecida',respect:s?V.qual.relation(s.respect||0):'desconhecido',condition:sim.publicCondition(p),uniform:p.military?'uniforme militar':null,estimated_goal:s?.estimatedGoal||null});if(out.length>=12)break}return out};

// Um único ciclo cognitivo por agente: corpo/intenção por frame; percepção espacial vem do sensorPulse v0.9.
function bodyEventScan(p){const s=p._perfBrain||(p._perfBrain={bodySig:'',wounds:(p.wounds||[]).length,novelty:p.brain.novelty||0,lastPeriod:-1});const sig=`${p.health<30?3:p.health<50?2:p.health<72?1:0}|${p.infection>.65?3:p.infection>.35?2:p.infection>.1?1:0}|${p.bleeding>2?2:p.bleeding>.5?1:0}|${p.thirst>85||p.hunger>88||p.energy<12?2:p.thirst>70||p.hunger>74||p.energy<25?1:0}|${p.bodyClock?.sleepPressure>90?2:p.bodyClock?.sleepPressure>72?1:0}`;if(s.bodySig&&sig!==s.bodySig)V.markReplan?.(p,'body_threshold_changed',p.health<30||p.infection>.35||p.bleeding>2);s.bodySig=sig;const wc=(p.wounds||[]).length;if(wc>s.wounds)V.markReplan?.(p,'new_wound',true);s.wounds=wc;const nov=p.brain.novelty||0;if(nov-s.novelty>=3)V.markReplan?.(p,'novelty_accumulated',false);s.novelty=nov;const period=Math.floor((((sim.minute%1440)+1440)%1440)/360);if(s.lastPeriod<0)s.lastPeriod=period;else if(period!==s.lastPeriod){s.lastPeriod=period;V.markReplan?.(p,'new_day_period',false)}const minute=((sim.minute%1440)+1440)%1440;if(minute>=1260&&p.brain.lastIdentityDay<sim.day)p.brain.identityDueDay=sim.day}
sim.scheduleAgentBrain=function(p){if(p?.type!=='agent'||!p.alive)return;bodyEventScan(p);V.maintainIntent?.(p);if(!p.brain.initialPlanRequested){p.brain.initialPlanRequested=true;V.markReplan?.(p,'initial_plan',false)}};

// Infectados: percepção/eventos em pulsos escalonados; locomoção continua por frame.
function scentGoal(z){const c=world.cellAtWorld(z.x,z.y);let best=null,score=0;for(let oy=-3;oy<=3;oy++)for(let ox=-3;ox<=3;ox++){const s=sim.scentMap.get(`${c.gx+ox},${c.gy+oy}`);if(!s)continue;const age=sim.minute-s.minute,val=s.strength*Math.exp(-age/26)-Math.hypot(s.x-z.x,s.y-z.y)/700;if(val>score){score=val;best=s}}return best}
function zombieSense(z,st){perf.zombieSensePulses++;z.mutation=world.radiationAt(z.x,z.y,sim.minute)*.24;let t=st.perso