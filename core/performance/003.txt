lue:power,enumerable:false});out.push(o)}return out.sort((x,y)=>y._power-x._power).slice(0,10)}
function lightFor(a,site){const base=world.lightAt(sim.minute),torch=sim.itemQty(a,'torch')>0;let v=site&&base<.35?base*.72:base;if(torch&&base<.5)v=Math.max(v,.58);return clamp(v,.1,1)}
function visualCandidates(list,a,range,limit,point=e=>({x:e.x+(e.w||0)/2,y:e.y+(e.h||0)/2})){return list.map(e=>{const p=point(e),d2=(p.x-a.x)**2+(p.y-a.y)**2;return{e,p,d2}}).sort((x,y)=>x.d2-y.d2).slice(0,limit*2.2).filter(q=>sim.canSee(a,q.e,range)).slice(0,limit)}
function computeSense(a){
  perf.sensorBuilds++;V.ensurePerson(a);const site=sim.currentSite(a),light=lightFor(a,site),weather=world.weatherAt(sim.minute),state=clamp(1-(a.bodyClock?.sleepPressure||0)*.003-(a.pain||0)*.002-(a.stress||0)*.0012,.42,1),range=(175+(a.attr?.perception||4)*29+sim.skillLevel(a,'foraging')*4)*(.34+.66*light)*state*(1-weather.rain*.18);
  const people=visualCandidates(sim.peopleGrid.near(a.x,a.y,range).filter(p=>p.id!==a.id&&p.alive),a,range,12).map(({e:p})=>{const known=a.socialModel?.has(p.id)||a.relations?.has(p.id)||Math.hypot(p.x-a.x,p.y-a.y)<65,s=a.socialModel?.get(p.id);return{id:p.id,name:known?p.name:'pessoa desconhecida',distance:V.qual.distance(Math.hypot(p.x-a.x,p.y-a.y)),direction:V.dirName(p.x-a.x,p.y-a.y),condition:sim.publicCondition(p),relationship:known?V.qual.relation(s?.affinity??a.relations?.get(p.id)??0):'desconhecida',uniform:p.military?'uniforme militar':null}});
  const zombies=visualCandidates(sim.zombieGrid.near(a.x,a.y,range*1.06).filter(z=>z.alive),a,range*1.06,10),knew=!!a.knowledge?.zombieSeen,figures=zombies.map(({e:z})=>({id:z.id,distance:V.qual.distance(Math.hypot(z.x-a.x,z.y-a.y)),direction:V.dirName(z.x-a.x,z.y-a.y),appearance:knew?'figura humana agressiva com padrão já testemunhado':'pessoa com ferimentos graves e comportamento anormal',recognition:knew?'ameaça anormal reconhecida':'não compreendido'}));
  const siteCandidates=world.nearbySites(a.x,a.y,range*1.25).map(s=>({s,cx:s.x+s.w/2,cy:s.y+s.h/2})).sort((x,y)=>((x.cx-a.x)**2+(x.cy-a.y)**2)-((y.cx-a.x)**2+(y.cy-a.y)**2)).slice(0,18),visibleSites=[];for(const q of siteCandidates){if(site?.id===q.s.id||sim.canSee(a,{x:q.cx,y:q.cy,id:q.s.id},range*1.25)){visibleSites.push(q);if(visibleSites.length>=10)break}}
  const sites=visibleSites.map(q=>({id:q.s.id,name:q.s.name,type:q.s.type,distance:V.qual.distance(Math.hypot(q.cx-a.x,q.cy-a.y)),direction:V.dirName(q.cx-a.x,q.cy-a.y),condition:q.s.fire>0?'em chamas':q.s.integrity<55?'visivelmente danificado':'aparentemente íntegro'}));
  const veh=visualCandidates(world.nearbyVehicles(a.x,a.y,range*1.1),a,range*1.1,6)