.map(({e:v})=>({id:v.id,name:v.name,distance:V.qual.distance(Math.hypot(v.x-a.x,v.y-a.y)),direction:V.dirName(v.x-a.x,v.y-a.y),condition:v.condition>65?'aparenta funcionar':v.condition>30?'aparenta danificado':'muito danificado'}));
  let containers=[],openings=[],room=null;if(site){const rr=sim.currentRoom?.(a);room=rr?.name||null;containers=(site.containers||[]).map(c=>({c,d2:(c.x-a.x)**2+(c.y-a.y)**2})).filter(q=>q.d2<(range*.55)**2).sort((x,y)=>x.d2-y.d2).slice(0,12).filter(q=>world.rayClear(a.x,a.y,q.c.x,q.c.y,true)).slice(0,8).map(q=>({id:q.c.id,name:q.c.name,room:q.c.room,distance:V.qual.distance(Math.sqrt(q.d2)),searched:!!q.c.searched,locked:!!q.c.locked}));openings=[...(site.doors||[]),...(site.windows||[])].map(o=>({o,d2:(o.x-a.x)**2+(o.y-a.y)**2})).filter(q=>q.d2<(range*.65)**2).sort((x,y)=>x.d2-y.d2).slice(0,16).filter(q=>world.rayClear(a.x,a.y,q.o.x,q.o.y,true)).map(q=>({id:q.o.id,type:q.o.openingType,distance:V.qual.distance(Math.sqrt(q.d2)),locked:!!q.o.locked,barricaded:(q.o.barricade||0)>0,open:!!q.o.open,integrity:q.o.integrity>65?'forte':q.o.integrity>30?'danificada':'quase cedendo'}))}
  const result={vision:{facing:V.dirName(Math.cos(a.facing||0),Math.sin(a.facing||0)),people,unidentified_figures:figures,sites,vehicles:veh,containers,openings,room},hearing:hearingFor(a),own_noise:a.vehicleId?'alto':a.action?.tool?.includes('breach')?'muito alto':a.action?.tool==='attack'?'moderado':a.action?'baixo':'quase silencioso',terrain:world.terrainAt(a.x,a.y),weather:weather.label,light:light<.3?'escuro':light<.65?'penumbra':'claro'};Object.defineProperty(result,'_visibleZombieIds',{value:zombies.map(q=>q.e.id),enumerable:false});Object.defineProperty(result,'_visibleSiteRefs',{value:visibleSites.map(q=>q.s),enumerable:false});return result
}
function pureSense(a){V.ensurePerson(a);const now=performance.now(),cell=`${Math.floor(a.x/W.CELL)},${Math.floor(a.y/W.CELL)},${Math.round((a.facing||0)*4)}`,c=a._senseCache;if(c&&c.cell===cell&&now-c.at<320&&c.noiseRev===(perf.noiseRevision||0))return c.data;const data=computeSense(a);a._senseCache={at:now,cell,noiseRev:perf.noiseRevision||0,data};return data}
sim.senseAgent=function(a,passive=false){const data=pureSense(a);if(!passive){const id=data._visibleZombieIds?.[0];if(id){const z=sim.entityById(id);if(z)sim.recordZombieSight(a,z,a.knowledge?.zombieSeen?'repeat':'sight')}for(const s of data._visibleSiteRefs||[])V.learnPlace(a,s)}return data};sim.observeSense=a=>sim.senseAgent(a,true);sim.sense=a=>sim.senseAgent(a,false);

// Leitura social é pura: consultar contexto não cria uma relação que nunca existiu.
sim.socialContext=function(a){V.ensurePerson(a);c