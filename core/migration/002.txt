id)||0)*7+(noise.get(nid)||0)*8+(road?2.2:0)+gap*.35+AZ.rand01(sim.seed,nc.cx,nc.cy,61200+bucket)*.35;if(score>bestScore+.55){bestScore=score;best=nc}}if(!best&&c.latent>c.base*1.35){const opts=dirs.map(([dx,dy])=>cell(c.cx+dx,c.cy+dy)).filter(Boolean);best=opts.sort((a,b)=>(a.latent-a.base)-(b.latent-b.base))[0]||null}if(!best)continue;const stochastic=AZ.rand01(sim.seed,c.cx,c.cy,61300+bucket),move=Math.min(3,c.latent,Math.floor(c.latent*.075*factor+stochastic*.9));if(move<=0)continue;delta.set(sid,(delta.get(sid)||0)-move);const bid=k(best.cx,best.cy);delta.set(bid,(delta.get(bid)||0)+move);M.moved+=move}for(const[id,d]of delta){const[cx,cy]=parse(id),c=cell(cx,cy);if(c)c.latent=Math.max(0,c.latent+d)}M.ticks++}
function spawnIn(c){if(!c)return null;const loaded=world.peekChunk(c.cx,c.cy);if(!loaded){world.requestChunk(c.cx,c.cy,9,'zombie-realization');return null}for(let attempt=0;attempt<8;attempt++){const n=c.spawnSeq++,rx=.08+AZ.rand01(sim.seed,c.cx,c.cy,62000+n*3)*.84,ry=.08+AZ.rand01(sim.seed,c.cy,c.cx,62001+n*3)*.84,x=(c.cx+rx)*CS,y=(c.cy+ry)*CS,q=world.nearestWalkable(x,y,null,5);if(world.worldInBounds(q.x,q.y,32)&&!nearAnyHuman(q.x,q.y,85))return q}return null}
function realize(){let active=sim.zombies.reduce((n,z)=>n+(z.alive?1:0),0),spawnBudget=12;if(active>=M.activeCap)return;const targets=new Map();for(const p of people()){const q=chunkOf(p.x,p.y);for(let oy=-1;oy<=1;oy++)for(let ox=-1;ox<=1;ox++){const c=cell(q.cx+ox,q.cy+oy);if(c)targets.set(k(c.cx,c.cy),c)}