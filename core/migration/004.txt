.y),lx=((cp.x%CS)+CS)%CS,ly=((cp.y%CS)+CS)%CS;world.requestChunk(cx,cy,10,'resident');if(lx<180)world.requestChunk(cx-1,cy,7,'edge-prefetch');if(lx>CS-180)world.requestChunk(cx+1,cy,7,'edge-prefetch');if(ly<180)world.requestChunk(cx,cy-1,7,'edge-prefetch');if(ly>CS-180)world.requestChunk(cx,cy+1,7,'edge-prefetch');if(p.target){const t=world.clampPoint?world.clampPoint(p.target.x,p.target.y,32):p.target,tx=AZ.chunkCoord(t.x),ty=AZ.chunkCoord(t.y);world.requestChunk(tx,ty,8,'intent-target')}}}
const baseUpdate=sim.update.bind(sim);sim.update=function(dt){const frameStart=performance.now();prefetch();baseUpdate(dt);for(const p of sim.people)if(p.alive&&!world.worldInBounds(p.x,p.y,24)){const q=world.clampPoint(p.x,p.y,28);p.x=q.x;p.y=q.y;p.target=null;p.action=null;if(p.type==='agent')window.AZV8?.markReplan?.(p,'world_boundary',false)}for(const z of sim.zombies)if(z.alive&&!world.worldInBounds(z.x,z.y,20)){const q=world.clampPoint(z.x,z.y,24);z.x=q.x;z.y=q.y;z._perfZombie&&(z._perfZombie.gx=NaN,z._perfZombie.gy=NaN)}absorbExisting();const now=performance.now(),elapsed=Math.max(0,Math.min(120,sim.minute-M.lastMinute));if(!sim.paused&&now-lastMigrationReal>1100){lastMigrationReal=now;M.lastMinute=sim.minute;migrate(elapsed);abstractFar();realize()}const spent=performance.now()-frameStart;if(now>=nextGeneration&&world.generationQueue?.size){const made=world.processGenerationQueue(spent<8?1:0,3.5);if(made){const ms=world.generationStats.lastMs||0;nextGeneration=now+(ms>12?180:ms>7?