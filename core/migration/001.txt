bieBase=sim.makeZombie.bind(sim);sim.makeZombie=function(...args){const z=makeZombieBase(...args);z._migrationManaged=true;z._migrationFresh=true;return z};
function people(){return sim.people.filter(p=>p.alive)}
function nearAnyHuman(x,y,r){const r2=r*r;for(const p of sim.peopleGrid.near(x,y,r))if(p.alive&&(p.x-x)**2+(p.y-y)**2<=r2)return true;return false}
function edgeRoad(cx,cy,nx,ny){if(!inside(cx,cy)||!inside(nx,ny))return false;const e=world.chunkEdges(cx,cy),dx=nx-cx,dy=ny-cy;if(dx===1)return e.E;if(dx===-1)return e.W;if(dy===1)return e.S;if(dy===-1)return e.N;return false}
function migrate(elapsed){const humans=new Map(),noise=new Map();for(const p of people()){const q=chunkOf(p.x,p.y),id=k(q.cx,q.cy);humans.set(id,(humans.get(id)||0)+1)}for(const n of sim.noiseEvents||[]){if(n.expires<=sim.minute)continue;const q=chunkOf(n.x,n.y);if(!inside(q.cx,q.cy))continue;const id=k(q.cx,q.cy);noise.set(id,(noise.get(id)||0)+n.loudness)}const delta=new Map(),factor=Math.min(2.5,Math.max(.15,elapsed/30)),dirs=[[1,0],[-1,0],[0,1],[0,-1]],bucket=Math.floor(sim.minute/20);for(const c of M.cells.values()){if(c.latent<=0)continue;const sid=k(c.cx,c.cy),sourceScore=(humans.get(sid)||0)*7+(noise.get(sid)||0)*8+(c.latent>c.base?2:0);let best=null,bestScore=sourceScore;for(const[dX,dY]of dirs){const nc=cell(c.cx+dX,c.cy+dY);if(!nc)continue;const nid=k(nc.cx,nc.cy),road=edgeRoad(c.cx,c.cy,nc.cx,nc.cy),gap=Math.max(-4,Math.min(6,nc.base-(nc.latent+(delta.get(nid)||0)))),score=(humans.get(n