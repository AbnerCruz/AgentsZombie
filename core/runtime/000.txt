'use strict';
(()=>{
const sim=window.sim,AZ=window.AZ,V=window.AZV8;if(!sim||!AZ||!V)return;const C=V.combat,clamp=AZ.clamp;
const base={update:sim.update.bind(sim),updatePerson:sim.updatePerson.bind(sim),updateZombie:sim.updateZombie.bind(sim),advanceAction:sim.advanceAction.bind(sim),finishAction:sim.finishAction.bind(sim),execute:sim.executeAgentTool.bind(sim),drive:sim.advanceDrive.bind(sim),talk:sim.resolveTalk.bind(sim),sight:sim.recordZombieSight.bind(sim),makePerson:sim.makePerson.bind(sim),makeZombie:sim.makeZombie.bind(sim),deploy:sim.deployMilitary?.bind(sim),remember:sim.remember.bind(sim),kill:sim.killPerson.bind(sim)};
const DIR={N:[0,-1],NE:[.707,-.707],E:[1,0],SE:[.707,.707],S:[0,1],SW:[-.707,.707],W:[-1,0],NW:[-.707,-.707]};
const ONE=new Set(['consume','equip','craft','build','talk','aid','trade','rest','observe','enter_vehicle','exit_vehicle','refuel_vehicle','set_goal','wait','combat_stance']);
const MOVING=new Set(['move','go_to','flee','drive_vehicle','attack']);
sim.replayLog=sim.replayLog||[];
function isA(p){return V.isAgent(p)}
function brain(p){V.ensurePerson(p);return p.brain}
function mark(p,reason,urgent=false){if(!isA(p)||!p.alive)return;const b=brain(p),t=performance.now(),seq=(b.eventSeq=(b.eventSeq||0)+1),old=b.replanPending;if(old){if(urgent&&!old.urgent)b.replanPending={reason,urgent:true,at:t,seq};else if(t-old.at>900&&reason!==old.reason)b.replanPending={reason:old.reason+'+'+reason,urgent:old.urgent||urgent,at:old.at,seq};return}b.replanPending={reason,urgent:!!urgent,at:t,seq}}
V.markReplan=mark;
function cleanArgs(a={}){const o={...a};delete o._phase;delete o._localRoutine;return o}
function makeIntent(p,tool,args={},bridge=false){const b=brain(p),it={id:`it:${p.id}:${++p._v8.intentSeq}`,tool,args:cleanArgs(args),reason:String(args.reason||''),bridge,created:sim.minute,completedOnce:false,attempts:0};if(tool==='move'||tool==='bridge_move'){const d=DIR[args.direction]||[Math.cos(p.facing||0),Math.sin(p.facing||0)],n=clamp(+args.distance||160,20,1200);it.dest={x:p.x+d[0]*n,y:p.y+d[1]*n}}if(tool==='drive_vehicle'){const v=p.vehicleId&&sim.world.vehicleById(p.vehicleId),d=DIR[args.direction]||[1,0],n=clamp(+args.distance||320,80,5000),x=v?.x??p.x,y=v?.y??p.y;it.dest={x:x+d[0]*n,y:y+d[1]*n}}if(tool==='scavenge'){it.startQty=sim.itemQty(p,args.wanted)||0;it.startWeight=sim.inventoryWeight(p)}return it}
function bridgeIntent(p,prev){let ang=p.facing||0;if(prev?.dest){const dx=prev.dest.x-p.x,dy=prev.dest.y-p.y;if(Math.hypot(dx,dy)>2)ang=Math.atan2(dy,dx)}const names=['E','SE','S','SW','W','NW','N','NE'],baseDir=(Math.round(ang/(Math.PI/4))+8)%8,wiggle=((p.id.charCodeAt(p.id.length-1)||0)+Math.floor(sim.minute/8))%3-1,dir=names[(baseDir+wiggle+8)%8];return makeIntent(p,'bridge_move',{direction:dir,distance:130+(p.id.length%4)*24,reason:'continuar explorando localmente enquanto o plano não muda'},true)}
function targetDistance(p,t){return sim.distanceToEntity(p,t)}
function intentSatisfied(p,it){if(!it)return true;if(it.tool==='move'||it.tool==='bridge_move')return !!it.dest&&Math.hypot(it.dest.x-p.x,it.dest.y-p.y)<18;if(it.tool==='go_to'){const t=sim.entityById(it.args.target_id);return!t||targetDistance(p,t)<=22}if(it.tool==='flee'){const t=sim.entityById(it.args.threat_id);return!t||!t.alive||targetDistance(p,t)>560}if(it.tool==='drive_vehicle'){const v=p.vehicleId&&sim.world.vehicleById(p.vehicleId);return!v||!it.dest||Math.hypot(it.dest.x-v.x,it.dest.y-v.y)<24}if(it.tool==='attack'){const t=sim.entityById(it.args.target_id);return!t||!t.alive||targetDistance(p,t)>900||p.energy<6}if(it.tool==='scavenge'){const s=sim.findSiteById(it.args.site_id);if(!s||!(s.loot||[]).some(x=>x.qty>0)||sim.inventoryWeight(p)>=sim.maxCarry(p)-.35)return true;const wanted=String(it.args.wanted||'').trim();return wa