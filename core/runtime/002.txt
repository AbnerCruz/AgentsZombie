+dx/l*520,y:p.y+dy/l*520};p.action={tool:'flee',args:{...it.args,direction:dir,reason:it.reason},started:sim.minute,duration:8,progress:0,model:'local-intent'};return}if(it.tool==='attack'){C.start(p,it.args.target_id,it.args.style||'balanced',{reason:it.reason});if(!p.action&&intentSatisfied(p,it))finishIntent(p);return}if(it.tool==='drive_vehicle'){if(!p.vehicleId){finishIntent(p,'intention_invalidated');return}const v=sim.world.vehicleById(p.vehicleId),dx=it.dest.x-v.x,dy=it.dest.y-v.y,d=Math.hypot(dx,dy);base.execute(p,'drive_vehicle',{direction:direction(dx,dy),distance:Math.max(80,Math.min(5000,d)),reason:it.reason},local);return}if(it.tool==='enter_vehicle'){const v=sim.world.vehicleById(it.args.vehicle_id);if(!v||v.occupant&&v.occupant!==p.id){finishIntent(p,'intention_invalidated');return}if(p.vehicleId===v.id){it.completedOnce=true;finishIntent(p);return}const d=Math.hypot(v.x-p.x,v.y-p.y);if(d>55){p.target={x:v.x,y:v.y};p.action={tool:'move',args:{reason:it.reason,_intentApproach:true},started:sim.minute,duration:d/48,progress:0,model:'local-intent'};return}if(v.locked){p.action={tool:'unlock_vehicle_v08',args:{vehicle_id:v.id,reason:it.reason},started:sim.minute,duration:Math.max(2,9-sim.skillLevel(p,'mechanics')*.8),progress:0,model:'local-intent'};return}occupy(p,v);it.completedOnce=true;return}if(it.tool==='refuel_vehicle'){const v=sim.world.vehicleById(it.args.vehicle_id);if(!v){finishIntent(p,'intention_invalidated');return}const d=Math.hypot(v.x-p.x,v.y-p.y);if(!p.vehicleId&&d>65){p.target={x:v.x,y:v.y};p.action={tool:'move',args:{reason:it.reason,_intentApproach:true},started:sim.minute,duration:d/48,progress:0,model:'local-intent'};return}}
 if(it.tool==='craft'){const r=sim.craftingRecipes[it.args.recipe];if(!r||!sim.hasNeed(p,r.need)){finishIntent(p,'intention_blocked');return}const skill=sim.skillLevel(p,r.skill||'carpentry');p.action={tool:'craft_v08',args:{...it.args,reason:it.reason},started:sim.minute,duration:Math.max(2,r.minutes*(1.08-skill*.025)),progress:0,model:'local-intent'};return}if(it.tool==='combat_stance'){C.ensure(p).stance=it.args.stance||'balanced';C.ensure(p).guard=it.args.stance==='defensive'?.65:it.args.stance==='distance'?.38:it.args.stance==='aggressive'?.08:.22;it.completedOnce=true;return}base.execute(p,it.tool,{...it.args,reason:it.reason},local);if(!p.action)it.completedOnce=true}
function routineOverride(p){const b=brain(p),bc=p.bodyClock,c=C.ensure(p);if(c.grabbedBy&&sim.minute<c.grabbedUntil){if(!p.action&&(!bc.lastEscape||sim.minute-bc.lastEscape>.25)){bc.lastEscape=sim.minute;C.escapeGrab(p)}return true}const z=sim.zombieGrid.near(p.x,p.y,34).find(x=>x.alive);if(z&&!p.vehicleId&&p.action?.tool!=='attack_v08'&&p.action?.tool!=='flee'){if(!p.action||p.action.args?._localRoutine){const dx=p.x-z.x,dy=p.y-z.y,l=Math.hypot(dx,dy)||1;p.target={x:p.x+dx/l*95,y:p.y+dy/l*95};p.action={tool:'reflex_flee',args:{reason:'recuo reflexo diante de agressão imediata',_localRoutine:true},started:sim.minute,duration:1.5,progress:0,model:'local-reflex'};p.stress=clamp(p.stress+3,0,100)}return true}if(p.action)return false;if(p.bleeding>1.4&&sim.itemQty(p,'bandage')){base.execute(p,'consume',{item_id:'bandage',reason:'conter sangramento imediato',_localRoutine:true},{model:'local-routine'});return true}if(p.thirst>82&&sim.itemQty(p,'water')){base.execute(p,'consume',{item_id:'water',reason:'beber água carregada',_localRoutine:true},{model:'local-routine'});return true}if(p.hunger>86&&sim.itemQty(p,'food')){base.execute(p,'consume',{item_id:'food',reason:'comer alimento carregado',_localRoutine:true},{model:'local-routine'});return true}const mod=((sim.minute%1440)+1440)%1440,night=mod>=1260||mod<390,needSleep=bc.sleepPressure>82||(night&&bc.sleepPressure>64);if(needSleep){const site=sim.currentSit