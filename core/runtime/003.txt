e(p);if(site){base.execute(p,'rest',{hours:1,reason:'sono fisiológico',_localRoutine:true},{model:'local-routine'});bc.sleeping=true;return true}const near=sim.world.nearbySites(p.x,p.y,420).filter(s=>s.fire<=0).sort((a,b)=>targetDistance(p,a)-targetDistance(p,b))[0];if(near){p.target={x:near.x+near.w/2,y:near.y+near.h/2};p.action={tool:'move',args:{reason:'procurar abrigo para dormir',_localRoutine:true},started:sim.minute,duration:Math.max(2,targetDistance(p,near)/42),progress:0,model:'local-routine'};bc.sleepSeeking=near.id;return true}if(bc.sleepPressure>94){base.execute(p,'rest',{hours:.5,reason:'exaustão física',_localRoutine:true},{model:'local-routine'});bc.sleeping=true;return true}}bc.sleeping=false;return false}
function maintain(p){if(!isA(p)||!p.alive)return;V.ensurePerson(p);if(routineOverride(p))return;const b=brain(p);if(!b.intent)b.intent=bridgeIntent(p);if(p.action)return;if(intentSatisfied(p,b.intent)){finishIntent(p);if(p.action)return}executeIntent(p,b.intent)}
V.maintainIntent=maintain;
function bodyTick(p,dt){V.ensurePerson(p);const bc=p.bodyClock,rest=p.action?.tool==='rest';if(rest){bc.sleepPressure=clamp(bc.sleepPressure-dt*.2,0,100);bc.sleeping=true}else{const mod=((sim.minute%1440)+1440)%1440,night=mod>=1320||mod<360;bc.sleepPressure=clamp(bc.sleepPressure+dt*(night?.09:.065),0,100);bc.sleeping=false}if(bc.sleepPressure>78){p.stress=clamp(p.stress+dt*.012,0,100);p.mood=clamp(p.mood-dt*.01,0,100)}if(bc.sleepPressure>94)p.energy=clamp(p.energy-dt*.018,0,100);if(bc.illness>0){bc.fever=clamp(bc.fever+dt*.002-bc.illness*dt*.0005,0,1);p.energy=clamp(p.energy-dt*bc.illness*.015,0,100);p.thirst=clamp(p.thirst+dt*bc.illness*.01,0,100);if(rest&&p.thirst<70)bc.illness=clamp(bc.illness-dt*.00035,0,1)}let leg=0;for(const w of p.wounds||[]){w.healing=w.healing||0;if(!w.infected){w.bleeding=Math.max(0,(w.bleeding||0)-dt*.0007);w.healing=clamp(w.healing+dt*(rest?.0011:.00055)*(p.hunger<70&&p.thirst<70?1:.55),0,1)}if(w.zone==='perna'&&w.severity>.35)leg=Math.max(leg,w.severity)}const scarLeg=(bc.scars||[]).some(s=>s.zone==='perna'&&s.severity>.7);bc.mobility=clamp(1-leg*.34-(scarLeg?.08:0)-(bc.sleepPressure>90?.12:0),.45,1);const healed=[];p.wounds=(p.wounds||[]).filter(w=>{if(w.healing>=1&&!w.infected){if(w.severity>.62)bc.scars.push({type:w.type,zone:w.zone||'corpo',severity:w.severity,day:sim.day});healed.push(w);return false}return true});if(healed.length)sim.remember(p,'recovery',`${healed.length} ferimento(s) cicatrizaram.`,5,['body','recovery'])}
function scan(p){if(!isA(p)||!p.alive)return;const s=p._v8,t=performance.now();if(t-s.sensorAt<380+(p.id.length%5)*45)return;s.sensorAt=t;const sensed=sim.senseAgent(p,true),ids=new Set((sensed.vision?.unidentified_figures||[]).map(x=>x.id));let fresh=false,near=false;for(const q of sensed.vision?.unidentified_figures||[]){if(!s.threatIds.has(q.id))fresh=true;if(q.distance==='muito perto'||q.distance==='perto')near=true}if(fresh)mark(p,'new_perceived_threat',near);s.threatIds=ids;const sig=`${p.health<30?3:p.health<50?2:p.health<72?1:0}|${p.infection>.65?3:p.infection>.35?2:p.infection>.1?1:0}|${p.bleeding>2?2:p.bleeding>.5?1:0}|${p.thirst>85||p.hunger>88||p.energy<12?2:p.thirst>70||p.hunger>74||p.energy<25?1:0}|${p.bodyClock.sleepPressure>90?2:p.bodyClock.sleepPressure>72?1:0}`;if(s.bodySig&&sig!==s.bodySig)mark(p,'body_threshold_changed',p.health<30||p.infection>.35||p.bleeding>2);s.bodySig=sig;const wc=(p.wounds||[]).length;if(wc>s.wounds)mark(p,'new_wound',true);s.wounds=wc;const nov=p.brain.novelty||0;if(nov-s.novelty>=3)mark(p,'novelty_accumulated');s.novelty=nov;const period=Math.floor((((sim.minute%1440)+1440)%1440)/360);if(p.brain.lastPlanPeriod<0)p.brain.lastPlanPeriod=period;else if(period!==p.brain.lastPlanPeriod){p.brain.lastPlanPeriod=period;mark(p,'new_day