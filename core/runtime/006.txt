,'ruído de trabalho manual')}return}if(a.tool==='unlock_vehicle_v08'){const v=sim.world.vehicleById(a.args.vehicle_id);if(!v||v.occupant)return;const chance=clamp(.38+sim.skillLevel(p,'mechanics')*.085+p.attr.intellect*.025,.4,.94);sim.practice(p,'mechanics',3);if(sim.rng.chance(chance)){v.locked=false;occupy(p,v)}else sim.remember(p,'vehicle',`Não conseguiu abrir ou ligar o ${v.name}.`,4,['vehicle',v.id]);return}return base.finishAction(p,a)};
function onActionEnded(p,a){if(!isA(p)||a.args?._localRoutine)return;const it=brain(p).intent;if(!it)return;if(a.tool==='attack_v08'||a.tool==='attack')return;if(ONE.has(it.tool)||it.tool==='craft')it.completedOnce=true;if(it.tool==='scavenge'||it.tool==='loot_vehicle')it.attempts++;if(intentSatisfied(p,it))finishIntent(p)}
sim.advanceAction=function(p,dt){V.ensurePerson(p);const before=p.action,ox=p.x,oy=p.y,mob=p.bodyClock?.mobility||1,move=before&&(MOVING.has(before.tool)||before.args?._phase==='approach'||before.tool==='reflex_flee'),scaled=move?dt*mob:dt;base.advanceAction(p,scaled);const dx=p.x-ox,dy=p.y-oy;if(Math.hypot(dx,dy)>.02){p.facing=Math.atan2(dy,dx);p._v8.lastMotionAt=performance.now();p._v8.lastPos={x:p.x,y:p.y}}if(before&&!p.action)onActionEnded(p,before)};
sim.advanceDrive=function(p,a,dt){base.drive(p,a,dt);const v=p.vehicleId&&sim.world.vehicleById(p.vehicleId);if(v&&(!v._lastNoise||sim.minute-v._lastNoise>1.2)){v._lastNoise=sim.minute;sim.emitNoise(v.x,v.y,.7,'engine',p.id,'motor de veículo')}};
const baseZombieWound=sim.zombieWound.bind(sim);sim.zombieWound=function(z,p){const out=baseZombieWound(z,p);const w=p.wounds[p.wounds.length-1];if(w&&!w.zone)w.zone=sim.rng.pick(['braço','torso','perna']);C.zombieAfterWound(z,p);if(isA(p))mark(p,'attacked_and_wounded',true);return out};
sim.updatePerson=function(p,dt){V.ensurePerson(p);bodyTick(p,dt);base.updatePerson(p,dt);if(p.alive)C.tickEntity(p,dt)};sim.updateZombie=function(z,dt){C.ensure(z);base.updateZombie(z,dt);if(z.alive)C.tickEntity(z,dt)};
sim.scheduleAgentBrain=function(p){if(!isA(p)||!p.alive)return;scan(p);maintain(p);if(!p.brain.initialPlanRequested){p.brain.initialPlanRequested=true;mark(p,'initial_plan',false)}};
sim.killPerson=function(p,cause){const was=p.alive;base.kill(p,cause);if(was&&!p.alive){for(const q of sim.peopleGrid.near(p.x,p.y,300))if(isA(q)&&q.alive)mark(q,'witnessed_death',true)}};
let lastDay=sim.day;sim.update=function(dt){const before=sim.day;base.update(dt);if(sim.day!==before||sim.day!==lastDay){const ended=sim.day-1;for(const p of sim.people){V.ensurePerson(p);V.consolidateMemory(p,ended);if((p.wounds||[]).some(w=>!w.infected&&w.severity>.72)&&sim.rng.chance(.07))p.bodyClock.illness=clamp(p.bodyClock.illness+.3,0,1)}lastDay=sim.day}};
for(const p of sim.people){V.ensurePerson(p);C.ensure(p);p.brain.taskQueue=[];if(isA(p)&&!p.brain.intent)p.brain.intent=bridgeIntent(p)}for(const z of sim.zombies)C.ensure(z);for(const s of sim.structures||[])if(!s.kind)s.kind='structure';
let cursor=0;setInterval(()=>{if(sim.paused)return;const agents=sim.people.filter(p=>isA(p)&&p.alive);if(!agents.length)return;for(let n=0;n<agents.length;n++){if(sim.ai.activeBrains.size>=sim.ai.maxConcurrent)break;const p=agents[(cursor+n)%agents.length],b=brain(p);if(b.pending||performance.now()<(b.retryAfter||0))continue;if(b.identityDueDay&&b.lastIdentityDay<b.identityDueDay