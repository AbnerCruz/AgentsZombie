_period',false)}const minute=((sim.minute%1440)+1440)%1440;if(minute>=1260&&p.brain.lastIdentityDay<sim.day)p.brain.identityDueDay=sim.day}
function senseAgent(a,write=false){V.ensurePerson(a);const range=170+a.attr.perception*28+sim.skillLevel(a,'foraging')*4,people=[],figures=[],sites=[],vehicles=[];for(const p of sim.peopleGrid.near(a.x,a.y,range)){if(p.id===a.id||!sim.canSee(a,p,range))continue;const sp=V.social(a,p),known=sp.familiarity>8||Math.hypot(p.x-a.x,p.y-a.y)<70;people.push({id:p.id,name:known?p.name:'pessoa desconhecida',distance:V.qual.distance(Math.hypot(p.x-a.x,p.y-a.y)),direction:V.dirName(p.x-a.x,p.y-a.y),condition:sim.publicCondition(p),relationship:{affinity:V.qual.relation(sp.affinity),trust:V.qual.relation(sp.trust),fear:V.qual.need(sp.fear)},uniform:p.military?'uniforme militar':null})}
 const seenZ=sim.zombieGrid.near(a.x,a.y,range*1.08).filter(z=>z.alive&&sim.canSee(a,z,range*1.08)).slice(0,10),knew=!!a.knowledge?.zombieSeen;if(write&&seenZ.length)sim.recordZombieSight(a,seenZ[0],knew?'repeat':'sight');for(const z of seenZ)figures.push({id:z.id,distance:V.qual.distance(Math.hypot(z.x-a.x,z.y-a.y)),direction:V.dirName(z.x-a.x,z.y-a.y),appearance:knew?'figura humana agressiva com padrão semelhante ao já testemunhado':'pessoa com ferimentos graves, postura anormal e comportamento imprevisível',recognition:knew?(a.lexicon?.threat||'ameaça anormal reconhecida'):'não compreendido'});
 for(const s of sim.world.nearbySites(a.x,a.y,range*1.25)){const q={x:s.x+s.w/2,y:s.y+s.h/2,id:s.id,w:0,h:0};if(!sim.canSee(a,q,range*1.25))continue;sites.push({id:s.id,name:s.name,type:s.type,distance:V.qual.distance(Math.hypot(q.x-a.x,q.y-a.y)),direction:V.dirName(q.x-a.x,q.y-a.y),condition:s.fire>0?'em chamas':s.barricade>0?'barricado':'sem alteração óbvia'});if(write)V.learnPlace(a,s)}for(const v of sim.world.nearbyVehicles(a.x,a.y,range*1.15)){if(!sim.canSee(a,v,range*1.15))continue;vehicles.push({id:v.id,name:v.name,distance:V.qual.distance(Math.hypot(v.x-a.x,v.y-a.y)),direction:V.dirName(v.x-a.x,v.y-a.y),condition:v.condition>65?'aparenta funcionar':v.condition>30?'aparenta danificado':'muito danificado'})}
 sim.noiseEvents=sim.noiseEvents.filter(n=>n.expires>sim.minute);const hearing=[];for(const n of sim.noiseEvents){if(n.sourceId===a.id)continue;const d=Math.hypot(n.x-a.x,n.y-a.y),reach=90+n.loudness*(480+a.attr.perception*38);if(d<=reach)hearing.push({sound:n.label||n.kind,direction:V.dirName(n.x-a.x,n.y-a.y),distance:V.qual.distance(d),intensity:n.loudness>.85?'muito alto':n.loudness>.45?'alto':n.loudness>.2?'moderado':'baixo'})}if(a.bodyClock.sleepPressure>96&&((Math.floor(sim.minute)+a.id.length)%19===0))hearing.push({sound:'som indistinto e possivelmente imaginado',direction:'incerta',distance:'incerta',intensity:'baixo',uncertain:true});const cues=[];if(sim.day>=3&&sim.world.explosionMinute<=sim.minute){const d=Math.hypot(a.x-sim.world.falloutOrigin.x,a.y-sim.world.falloutOrigin.y);if(d<1600)cues.push('fumaça distante e partículas no ar');if(d<700)cues.push('gosto metálico e irritação física')}return{vision:{facing:V.dirName(Math.cos(a.facing||0),Math.sin(a.facing||0)),people:people.slice(0,12),unidentified_figures:figures,sites:sites.slice(0,8),vehicles:vehicles.slice(0,6)},hearing:hearing.slice(0,10),own_noise:a.vehicleId?'alto':a.action?.tool?.includes('attack')?'moderado':a.action?'baixo':'quase silencioso',terrain:sim.world.terrainAt(a.x,a.y),environmental_cues:cues}}
sim.senseAgent=senseAgent;sim.observeSense=a=>senseAgent(a,false);sim.sense=a=>senseAgent(a,!!sim._agentSenseWrite);
sim.socialContext=function(a){V.ensurePerson(a);return sim.peopleGrid.near(a.x,a.y,260).filter(p=>p.id!==a.id).slice(0,12).map(p=>{const s=V.social(a,p);return{id:p.id,name:s.familiarity>8?p.name:'pessoa desconhecida',affinity:V.qua