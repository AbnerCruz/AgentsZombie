nted?sim.itemQty(p,wanted)>it.startQty:false}if(it.tool==='loot_vehicle'){const v=sim.world.vehicleById(it.args.vehicle_id);return!v||!(v.trunk||[]).some(x=>x.qty>0)||sim.inventoryWeight(p)>=sim.maxCarry(p)-.35}if(it.tool==='enter_vehicle')return p.vehicleId===it.args.vehicle_id;if(it.tool==='exit_vehicle')return!p.vehicleId;if(it.tool==='equip')return p.equippedWeapon===it.args.item_id;if(ONE.has(it.tool))return !!it.completedOnce;return false}
function serialize(it){if(!it)return null;return{tool:it.tool==='bridge_move'?'move':it.tool,reason:it.reason||'',target:it.args.target_id||it.args.site_id||it.args.vehicle_id||null,bridge:!!it.bridge,created:sim.formatMinute(it.created)}}sim.serializeIntent=serialize;
function finishIntent(p,why='intention_completed'){const b=brain(p),old=b.intent;b.intent=null;if(b.nextIntent){b.intent=b.nextIntent;b.nextIntent=null;return}if(!old?.bridge)mark(p,why,false);b.intent=bridgeIntent(p,old)}
function urgentFor(p,tool,args,event){if(event?.urgent||tool==='flee'||p.health<22||p.bleeding>2.4)return true;if(tool==='attack'){const t=sim.entityById(args.target_id);return!!t&&targetDistance(p,t)<95}return tool==='consume'&&args.item_id==='bandage'&&p.bleeding>1.4}
sim.applyBrainIntent=function(p,tool,args={},meta={}){if(!isA(p)||!p.alive)return false;const b=brain(p),it=makeIntent(p,tool,args),urgent=urgentFor(p,tool,args,meta.event);b.plan={summary:it.reason||`${tool}`,tool,target:it.args.target_id||it.args.site_id||it.args.vehicle_id||null,created:sim.minute,period:Math.floor(((sim.minute%1440)+1440)%1440/360)};if(!b.intent||b.intent.bridge){if(p.action&&p.action.args?._localRoutine!==true){p.action=null;p.target=null}b.intent=it;b.nextIntent=null}else if(p.action&&!urgent)b.nextIntent=it;else if(!urgent&&!intentSatisfied(p,b.intent))b.nextIntent=it;else{if(urgent&&p.action){p.action=null;p.target=null}b.intent=it;b.nextIntent=null}if(!b.replanPending||!meta.event?.seq||b.replanPending.seq<=meta.event.seq)b.replanPending=null;b.novelty=Math.max(0,(b.novelty||0)-3);return true};
function direction(dx,dy){return['E','SE','S','SW','W','NW','N','NE'][(Math.round(Math.atan2(dy,dx)/(Math.PI/4))+8)%8]}
function occupy(p,v){v.occupant=p.id;v.dirty=true;p.vehicleId=v.id;p.x=v.x;p.y=v.y;p.target=null;sim.remember(p,'vehicle',`Entrou no ${v.name}.`,4,['vehicle',v.id])}
function executeIntent(p,it){if(!it||p.action||!p.alive)return;if(intentSatisfied(p,it)){finishIntent(p);return}const local={model:'local-intent',localIntent:true,forceImmediate:true};if(it.tool==='move'||it.tool==='bridge_move'){const d=Math.hypot(it.dest.x-p.x,it.dest.y-p.y);if(p.vehicleId){const v=sim.world.vehicleById(p.vehicleId),dx=it.dest.x-v.x,dy=it.dest.y-v.y;base.execute(p,'drive_vehicle',{direction:direction(dx,dy),distance:Math.max(80,d),reason:it.reason},local);return}p.target={...it.dest};p.action={tool:'move',args:{direction:it.args.direction,reason:it.reason},started:sim.minute,duration:Math.max(1,d/48),progress:0,model:'local-intent'};return}if(it.tool==='go_to'){const t=sim.entityById(it.args.target_id);if(!t){finishIntent(p,'intention_invalidated');return}const d=targetDistance(p,t);if(p.vehicleId&&d>80){const v=sim.world.vehicleById(p.vehicleId),tx=t.x+(t.w||0)/2,ty=t.y+(t.h||0)/2;base.execute(p,'drive_vehicle',{direction:direction(tx-v.x,ty-v.y),distance:Math.max(80,Math.min(5000,d)),reason:it.reason},local);return}base.execute(p,'go_to',{...it.args,reason:it.reason},local);if(!p.action&&intentSatisfied(p,it))finishIntent(p);return}if(it.tool==='flee'){const t=sim.entityById(it.args.threat_id);if(!t||!t.alive){finishIntent(p);return}const dx=p.x-t.x,dy=p.y-t.y,l=Math.hypot(dx,dy)||1,dir=direction(dx,dy);if(p.vehicleId){base.execute(p,'drive_vehicle',{direction:dir,distance:650,reason:it.reason},local);return}p.target={x:p.x