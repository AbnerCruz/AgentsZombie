l.relation(s.affinity),trust:V.qual.relation(s.trust),fear:V.qual.need(s.fear),familiarity:V.qual.need(s.familiarity),condition:sim.publicCondition(p),uniform:p.military?'uniforme militar':null,estimated_goal:s.estimatedGoal||null}})};
sim.remember=function(p,kind,text,importance=5,tags=[]){V.ensurePerson(p);if(kind==='thought'){p.brain.lastThought=String(text||'');return null}const arr=p.memory?.entries||[],last=arr[arr.length-1];if(last&&last.kind===kind&&last.text===String(text)&&sim.minute-last.minute<8)return last;return base.remember(p,kind,text,importance,tags)};
sim.recordZombieSight=function(p,z,kind='sight'){const first=base.sight(p,z,kind);if(first)V.learnFact(p,'anomaly:violent-human','Uma pessoa extremamente ferida continuou avançando e atacando apesar do estado do corpo.','sensor',.9,{firsthand:true});else if(p.knowledge?.zombieSeen)V.learnFact(p,'anomaly:violent-human','O padrão de pessoas gravemente feridas e agressivas parece se repetir.','sensor',.84,{firsthand:true});if(isA(p))mark(p,first?'first_unexplained_contact':'recognized_threat_seen',first);return first};
sim.resolveTalk=function(a,b,message,intent){const r=base.talk(a,b,message,intent);V.ensurePerson(a);V.ensurePerson(b);const ab=V.social(a,b),ba=V.social(b,a),hostile=/ameaça|roub|calar|idiota|covarde|sai daqui/i.test(message);ab.familiarity=clamp(ab.familiarity+2,0,100);ba.familiarity=clamp(ba.familiarity+3,0,100);ab.lastInteraction=ba.lastInteraction=sim.minute;if(hostile){ba.trust=clamp(ba.trust-12,-100,100);ba.fear=clamp(ba.fear+8,0,100)}else{ab.trust=clamp(ab.trust+1,-100,100);ba.trust=clamp(ba.trust+2,-100,100)}if(intent)ba.estimatedGoal=String(intent);if(/warn|inform|aviso|alert/i.test(String(intent))||String(message).length>20){const key=V.topicKey(message),conf=clamp(.35+(ba.trust+100)/400,0.2,.82);V.learnFact(b,key,message,a.id,conf);ba.knownBeliefs.add(key)}const share=[...a.beliefs.values()].filter(x=>x.confidence>.58).sort((x,y)=>y.lastUpdated-x.lastUpdated).slice(0,2);for(const fact of share){const conf=clamp(fact.confidence*(.35+(ba.trust+100)/330),.12,.86);V.learnFact(b,fact.key,fact.content,a.id,conf);ba.knownBeliefs.add(fact.key)}if(isA(b))mark(b,'heard_relevant_speech',false);if(isA(a))mark(a,'conversation_changed',false);return r};
sim.makePerson=function(i,type,opts){const p=base.makePerson(i,type,opts);V.ensurePerson(p);C.ensure(p);return p};sim.makeZombie=function(x,y,source){const z=base.makeZombie(x,y,source);C.ensure(z);return z};if(base.deploy)sim.deployMilitary=function(...args){const r=base.deploy(...args);for(const s of sim.structures||[])if(!s.kind)s.kind='structure';return r};
sim.executeAgentTool=function(p,name,args={},meta={}){if(!p?.alive)return false;V.ensurePerson(p);if(meta.brainIntent)return sim.applyBrainIntent(p,name,args,meta);if(name==='attack')return C.start(p,args.target_id,args.style||'balanced',{reason:args.reason});if(name==='combat_stance'){const c=C.ensure(p);c.stance=args.stance||'balanced';c.guard=c.stance==='defensive'?.65:c.stance==='distance'?.38:c.stance==='aggressive'?.08:.22;return true}if(name==='craft'){const r=sim.craftingRecipes[args.recipe];if(!r||!sim.hasNeed(p,r.need))return false;p.action={tool:'craft_v08',args:{...args},started:sim.minute,duration:Math.max(2,r.minutes*(1.08-sim.skillLevel(p,r.skill||'carpentry')*.025)),progress:0,model:meta.model||'local'};return true}return base.execute(p,name,args,meta)};
sim.finishAction=function(p,a){if(a.tool==='attack_v08'){C.finish(p,a.args.target_id,a.args.style||'balanced');return}if(a.tool==='craft_v08'){const r=sim.craftingRecipes[a.args.recipe];if(r&&sim.consumeNeed(p,r.need)){sim.addItem(p,r.out,1);sim.practice(p,r.skill||'carpentry',5);sim.remember(p,'craft',`Fabricou ${r.name}.`,6,['craft',r.out.id]);sim.emitNoise(p.x,p.y,.22,'craft',p.id