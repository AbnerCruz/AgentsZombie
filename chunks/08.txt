const up=e=>{const wasMoved=this.pointer.moved;this.touches.delete(e.pointerId);if(this.touches.size<2)this.pinch=null;if(this.pointer.down&&!wasMoved&&this.touches.size===0)this.pick(e.clientX,e.clientY);this.pointer.down=false};
   c.addEventListener('pointerup',up);c.addEventListener('pointercancel',up);
   c.addEventListener('wheel',e=>{e.preventDefault();const old=this.sim.camera.zoom;const z=AZ.clamp(old*Math.exp(-e.deltaY*.001),.18,3);this.zoomAround(e.clientX,e.clientY,z)},{passive:false});
 }
 beginPinch(){const p=[...this.touches.values()];this.pinch={dist:Math.hypot(p[0].x-p[1].x,p[0].y-p[1].y),zoom:this.sim.camera.zoom,cx:(p[0].x+p[1].x)/2,cy:(p[0].y+p[1].y)/2}}
 doPinch(){if(!this.pinch)this.beginPinch();const p=[...this.touches.values()],d=Math.hypot(p[0].x-p[1].x,p[0].y-p[1].y),z=AZ.clamp(this.pinch.zoom*(d/Math.max(1,this.pinch.dist)),.18,3);this.zoomAround((p[0].x+p[1].x)/2,(p[0].y+p[1].y)/2,z)}
 zoomAround(sx,sy,newZoom){const r=this.canvas.getBoundingClientRect(),old=this.sim.camera.zoom,wx=this.sim.camera.x+(sx-r.left-r.width/2)/old,wy=this.sim.camera.y+(sy-r.top-r.height/2)/old;this.sim.camera.zoom=newZoom;this.sim.camera.x=wx-(sx-r.left-r.width/2)/newZoom;this.sim.camera.y=wy-(sy-r.top-r.height/2)/newZoom}
 screenToWorld(sx,sy){const r=this.canvas.getBoundingClientRect();return{x:this.sim.camera.x+(sx-r.left-r.width/2)/this.sim.camera.zoom,y:this.sim.camera.y+(sy-r.top-r.height/2)/this.sim.camera.zoom}}
 pick(sx,sy){const w=this.screenToWorld(sx,sy),rad=28/this.sim.camera.zoom;let best=null,bd=rad*rad;for(const p of this.sim.people){if(!p.alive)continue;const d=(p.x-w.x)**2+(p.y-w.y)**2;if(d<bd){bd=d;best=p}}for(const z of this.sim.zombies){if(!z.alive)continue;const d=(z.x-w.x)**2+(z.y-w.y)**2;if(d<bd){bd=d;best=z}}this.sim.selected=best;window.dispatchEvent(new CustomEvent('az-selection',{detail:best}))}
 centerOnSelected(){const s=this.sim.selected;if(s){this.sim.camera.x=s.x;this.sim.camera.y=s.y;this.follow=true}}
 render(){
   const s=this.sim,ctx=this.ctx,w=this.canvas.width/this.dpr,h=this.canvas.height/this.dpr;if(this.follow&&s.selected?.alive){s.camera.x=AZ.lerp(s.camera.x,s.selected.x,.09);s.camera.y=AZ.lerp(s.camera.y,s.selected.y,.09)}
   ctx.setTransform(this.dpr,0,0,this.dpr,0,0);ctx.fillStyle='#111713';ctx.fillRect(0,0,w,h);ctx.save();ctx.translate(w/2,h/2);ctx.scale(s.camera.zoom,s.camera.zoom);ctx.translate(-s.camera.x,-s.camera.y);
   const chunks=s.world.visibleChunks(s.camera,w,h);for(const c of chunks)this.drawChunk(c);this.drawStructures();this.drawCorpses();this.drawEntities();ctx.restore();this.drawVignette(w,h);
 }
 drawChunk(c){const ctx=this.ctx,cs=AZ.CHUNK_SIZE,x=c.cx*cs,y=c.cy*cs;ctx.fillStyle=c.biome==='forest'?'#233526':c.biome==='farmland'?'#38422a':c.biome==='marsh'?'#233936':'#2d3b2c';ctx.fillRect(x,y,cs+1,cs+1);
   // lightweight procedural texture, deterministic and zoom-friendly
   if(this.sim.camera.zoom>.32){for(let i=0;i<28;i++){const rx=AZ.rand01(this.sim.seed,c.cx,c.cy,900+i*2)*cs,ry=AZ.rand01(this.sim.seed,c.cx,c.cy,901+i*2)*cs;ctx.fillStyle=c.biome==='forest'?'#1c2d20':'rgba(10,20,12,.18)';ctx.fillRect(x+rx,y+ry,3,3)}}
   ctx.fillStyle='#454a43';if(c.roads.vertical)ctx.fillRect(x+cs/2-44,y,88,cs);if(c.roads.horizontal)ctx.fillRect(x,y+cs/2-44,cs,88);
   ctx.strokeStyle='rgba(225,218,164,.24)';ctx.lineWidth=3;ctx.setLineDash([22,24]);if(c.roads.vertical){ctx.beginPath();ctx.moveTo(x+cs/2,y);ctx.lineTo(x+cs/2,y+cs);ctx.stroke()}if(c.roads.horizontal){ctx.beginPath();ctx.moveTo(x,y+cs/2);ctx.lineTo(x+cs,y+cs/2);ctx.stroke()}ctx.setLineDash([]);
   for(const b of c.sites)this.drawSite(b);if(this.sim.overlays.rad)this.drawRadChunk(c);
 }
 drawSite(b){const ctx=this.ctx;const color={house:'#6b6252',store:'#6d704f',clinic:'#d7d3bd',garage:'#625f58',warehouse:'#555c59',farm:'#6f674a',barn:'#7a5144',cabin:'#5b4b3a',shed:'#5a5548',tower:'#666b67',waste:'#716e42'}[b.type]||'#625e53';ctx.fillStyle='rgba(0,0,0,.28)';ctx.fillRect(b.x+5,b.y+6,b.w,b.h);ctx.fillStyle=color;ctx.fillRect(b.x,b.y,b.w,b.h);ctx.fillStyle='rgba(10,12,11,.55)';ctx.fillRect(b.x+b.w*.4,b.y+b.h-12,b.w*.2,12);if(b.barricade>0){ctx.strokeStyle='#bd9364';ctx.lineWidth=3+5*b.barricade;ctx.strokeRect(b.x-2,b.y-2,b.w+4,b.h+4)}if(b.fire>0){ctx.fillStyle='rgba(231,102,48,.75)';ctx.fillRect(b.x+b.w*.25,b.y-8,12,18)}}
 drawRadChunk(c){const ctx=this.ctx,cs=AZ.CHUNK_SIZE,x=c.cx*cs,y=c.cy*cs;const v=this.sim.world.radiationAt(x+cs/2,y+cs/2,this.sim.minute);if(v<.035)return;ctx.fillStyle=`rgba(180,220,57,${Math.min(.35,v*.28)})`;ctx.fillRect(x,y,cs,cs)}
 drawStructures(){const ctx=this.ctx;for(const s of this.sim.structures){ctx.fillStyle=s.type==='campfire'?'#ef8b42':'#a58a61';ctx.fillRect(s.x-6,s.y-6,12,12)}}
 drawCorpses(){const ctx=this.ctx;ctx.fillStyle='#5e3d3a';for(const c of this.sim.corpses){if(c.reanimated)continue;ctx.fillRect(c.x-6,c.y-3,12,6)}}
 drawEntities(){const s=this.sim,ctx=this.ctx,z=s.camera.zoom;for(const e of s.zombies){if(!e.alive||!this.inView(e.x,e.y,80))continue;ctx.fillStyle=e.mutation>.35?'#a8dd52':'#70a95d';ctx.beginPath();ctx.arc(e.x,e.y,7,0,Math.PI*2);ctx.fill();if(s.selected===e){ctx.strokeStyle='#fff';ctx.lineWidth=2/z;ctx.beginPath();ctx.arc(e.x,e.y,12,0,Math.PI*2);ctx.stroke()}}
   for(const p of s.people){if(!p.alive||!this.inView(p.x,p.y,80))continue;ctx.fillStyle=p.type==='agent'?'#8bd3ff':'#e5ba67';ctx.beginPath();ctx.arc(p.x,p.y,p.type==='agent'?6.5:5.2,0,Math.PI*2);ctx.fill();if(p.infection>.55){ctx.strokeStyle='#8ad060';ctx.lineWidth=2;ctx.stroke()}if(s.overlays.needs){const danger=Math.max(p.hunger,p.thirst,100-p.health,p.stress);ctx.fillStyle=danger>70?'#d75c55':'#a7c27e';ctx.fillRect(p.x-8,p.y-12,16*(1-danger/100),2)}if(s.selected===p){ctx.strokeStyle='#fff';ctx.lineWidth=2/z;ctx.beginPath();ctx.arc(p.x,p.y,12,0,Math.PI*2);ctx.stroke()}if(s.overlays.names&&z>.45){ctx.font=`${10/z}px system-ui`;ctx.fillStyle='rgba(240,245,241,.8)';ctx.textAlign='center';ctx.fillText(p.name,p.x,p.y-12/z)}}
 }
 inView(x,y,pad){const r=this.canvas.get