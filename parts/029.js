 const p=o,isAgent=p.type==='agent',rel=[...p.relations.entries()].map(([id,v])=>({q:sim.entityById(id),v})).filter(x=>x.q).sort((a,b)=>Math.abs(b.v)-Math.abs(a.v)).slice(0,10),mem=p.memory.entries.slice(-12).reverse(),action=p.action?`<div class="actionBox"><small>AÇÃO ATUAL</small><b>${escapeHtml(p.action.tool)}</b><div>${escapeHtml(p.action.args?.reason||'')}</div></div>`:`<div class="actionBox"><small>AÇÃO ATUAL</small><b>${p.brain.pending?'pensando...':'aguardando decisão'}</b></div>`,weapon=sim.weaponStats(p),wounds=(p.wounds||[]).slice().reverse();
