'use strict';(function boot(){const sim=window.sim,ins=document.getElementById('inspector');if(!sim?._v074Ready||!ins){setTimeout(boot,50);return}const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const style=document.createElement('style');style.textContent='.v06diag{display:none!important}.v074stable{margin-top:12px}.v074stable.hidden{display:none}.v074stable .sensorGrid{display:grid;gap:7px}.v074stable .sensorBox,.v074stable .intentBox{padding:9px;border-radius:9px;background:rgba(255,255,255,.035);border:1px solid rgba(255,255,255,.07);min-height:52px}.v074stable small{display:block;opacity:.68;margin-top:4px;line-height:1.45}.v074stable h3{margin-top:14px}';document.head.appendChild(style);let panel=document.getElementById('stableAgentTelemetry');if(!panel){panel=document.createElement('div');panel.id='stableAgentTelemetry';panel.className='v074stable hidden';ins.insertAdjacentElement('afterend',panel)}const label=i=>i?`<b>${esc(i.tool==='bridge_move'?'movimento local':i.tool)}</b><small>${esc(i.reason||'sem descrição')}${i.bridge?' · ponte local sem IA':''}</small>`:'<small>nenhuma</small>';function render(){const p=sim.selected;if(!p||p.kind!=='person'){panel.classList.add('hidden');return}panel.classList.remove('hidden');const s=sim.sense(p),vision=[...(s.vision?.people||[]),...(s.vision?.unidentified_figures||[])].slice(0,8),hearing=s.hearing||[],b=p.brain||{};const next=`<h3>Intenção</h3><div class="intentBox">${label(b.intent)}</div><div class="intentBox"><b>PRÓXIMA INTENÇÃO</b>${label(b.nextIntent)}</div><h3>Sensores</h3><div class="sensorGrid"><div class="sensorBox"><b>VISÃO · ${esc(s.vision?.facing||'—')}</b><small>${vision.length?vision.map(v=>`${esc(v.name||v.appearance||v.recognition||'figura')} · ${esc(v.direction)} · ${esc(v.distance)}`).join('<br>'):'nada relevante no campo visual'}</small></div><div class="sensorBox"><b>AUDIÇÃO</b><small>${hearing.length?hearing.map(h=>`${esc(h.sound)} · ${esc(h.direction)} · ${esc(h.distance)} · ${esc(h.intensity)}`).join('<br>'):'nenhum som relevante'}</small></div><div class="sensorBox"><b>RUÍDO PRÓPRIO</b><small>${esc(s.own_noise||'—')}</small></div></div>`;if(panel.innerHTML!==next)panel.innerHTML=next}window.addEventListener('az-selection',render);setInterval(render,500);render()})();